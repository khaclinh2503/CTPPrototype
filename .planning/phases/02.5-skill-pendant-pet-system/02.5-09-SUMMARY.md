---
phase: 02.5-skill-pendant-pet-system
plan: "09"
subsystem: skills/handlers_start
tags: [skill, on_pass_start, grammy, mu_phep, passing_bonus, d47]
dependency_graph:
  requires: [02.5-01]
  provides: [SK_GRAMMY handler, SK_MU_PHEP handler, calc_total_passing_bonus]
  affects: [ctp/skills/registry.py, FSM on_pass_start integration]
tech_stack:
  added: []
  patterns: [always_active flag, additive stacking helper, random.choice unowned tile]
key_files:
  created:
    - ctp/skills/handlers_start.py
    - tests/test_handlers_start.py
  modified: []
decisions:
  - "MuPhep value computed from base_rate + (star - min_star) * chance dùng cùng RankConfig schema như các skill khác (base_rate = base value %)"
  - "calc_total_passing_bonus nhận list[dict] từ fire() call results — filter by type để bỏ qua grammy_claim và các result khác"
  - "so_10_passing_bonus_modifier giữ lại trong handlers_start.py như helper cho Plan 11, không register vào SKILL_HANDLERS (Plan 11 sẽ đảm nhiệm)"
metrics:
  duration: "~10 minutes"
  completed: "2026-04-06"
  tasks: 1
  files: 2
---

# Phase 2.5 Plan 09: SK_GRAMMY + SK_MU_PHEP (ON_PASS_START) Summary

**One-liner:** SK_GRAMMY claims random unowned CITY→L4 (nhà cấp 3, D-23); SK_MU_PHEP always-active % passing bonus; D-47 additive stacking helper.

## Tasks Completed

| # | Name | Status | Files |
|---|------|--------|-------|
| 1 | SK_GRAMMY + SK_MU_PHEP handlers | Done | ctp/skills/handlers_start.py, tests/test_handlers_start.py |

## What Was Built

### `ctp/skills/handlers_start.py`

- **`handle_grammy`**: ON_PASS_START trigger. Tìm tất cả CITY tiles chưa có owner, `random.choice` 1 tile, set `owner_id` + `building_level=4` (D-23 nhà cấp 3), gọi `player.add_property()`. Trả `{"type": "grammy_claim", "position": ...}` hoặc `None` nếu không còn tile trống. Rate check do engine xử lý (A: 16%+1%/star, S: 22%+2%/star).

- **`handle_mu_phep`**: ON_PASS_START trigger, `always_active=True` trong YAML config. Tính `value = base_rate + (star - min_star) * chance` từ `RankConfig`. Rank R → dùng S config (D-03). Trả `{"type": "passing_bonus_modifier", "percent_increase": value}`. Không có rate check — engine gọi trực tiếp do `cfg.always_active`.

- **`so_10_passing_bonus_modifier`**: Helper cho Plan 11. Cố định +50%, no rank/star scaling. Chưa đăng ký vào `SKILL_HANDLERS` (Plan 11 đảm nhiệm).

- **`calc_total_passing_bonus(base_bonus, modifiers)`**: D-47 additive stacking. Filter các dict có `type="passing_bonus_modifier"`, cộng dồn `percent_increase`, áp dụng một lần: `base * (1 + total_pct/100)`.

- Đăng ký: `SKILL_HANDLERS["SK_GRAMMY"]` và `SKILL_HANDLERS["SK_MU_PHEP"]`.

### `tests/test_handlers_start.py`

14 test functions — tất cả pass:

- Grammy: claims CITY + L4
- Grammy: no unowned → None
- Grammy: Resort tiles không bị ảnh hưởng
- Grammy: registered in SKILL_HANDLERS
- MuPhep: always active (5 lần call đều có result)
- MuPhep B1★: value=41
- MuPhep S5★: value=86
- MuPhep: type="passing_bonus_modifier" + A3★=57
- MuPhep: Rank R dùng S config (D-03)
- MuPhep: registered in SKILL_HANDLERS
- calc_total: 86% on 150k = 279k
- calc_total: 86%+50% on 150k = 354k (D-47 stacking)
- calc_total: no modifiers → base
- calc_total: ignore non-passing-bonus types

## Deviations from Plan

### Auto-added helper not in original plan

**1. [Rule 2 - Missing] `so_10_passing_bonus_modifier` helper**
- **Found during:** Task 1 implementation
- **Issue:** Plan task action đề cập "SK_SO_10 Effect 2 (also ON_PASS_START): Implemented here as a helper called from SO_10 handler (Plan 11)" — tuy nhiên không yêu cầu register ngay
- **Fix:** Thêm function vào file nhưng KHÔNG đăng ký vào SKILL_HANDLERS. Plan 11 sẽ đăng ký SK_SO_10 (Plan action nói "gọi từ SO_10 handler")
- **Files modified:** ctp/skills/handlers_start.py
- **Commit:** (trong cùng commit Task 1)

**2. Test count: 14 (vượt mức tối thiểu 7)**
- Thêm test coverage cho: Rank R→S config, A3★ value, ignore non-modifier types
- Giúp bảo vệ D-03 và D-47 tốt hơn

## Known Stubs

- `so_10_passing_bonus_modifier`: function có sẵn trong file nhưng chưa được wire vào SKILL_HANDLERS. Plan 11 sẽ gọi từ SK_SO_10 handler.

## Threat Flags

Không có threat flag mới — pure game logic, không có network/auth/file access.

## Self-Check

**Files exist:**
- `ctp/skills/handlers_start.py` — FOUND
- `tests/test_handlers_start.py` — FOUND

**Tests:** 14 passed / 0 failed

**Acceptance criteria:**
- `def handle_grammy` — FOUND (line 17)
- `def handle_mu_phep` — FOUND (line 51)
- `def calc_total_passing_bonus` — FOUND (line 106)
- `building_level = 4` — FOUND (line 42)
- `passing_bonus_modifier` — FOUND (multiple lines)
- `always_active` — FOUND (docstring + logic)
- Test count >= 7 — 14 tests (PASS)

## Self-Check: PASSED

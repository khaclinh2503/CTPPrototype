---
phase: 02.5-skill-pendant-pet-system
plan: 09
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - ctp/skills/handlers_start.py
  - tests/test_handlers_start.py
autonomous: true
requirements:
  - PLAY-02

must_haves:
  truths:
    - "SK_GRAMMY fires ON_PASS_START, builds on random empty tile or upgrades cheapest"
    - "SK_MU_PHEP fires ON_PASS_START, increases passing bonus by percentage"
  artifacts:
    - path: "ctp/skills/handlers_start.py"
      provides: "SK_GRAMMY, SK_MU_PHEP handlers"
      contains: "handle_sk_grammy"
    - path: "tests/test_handlers_start.py"
      provides: "Unit tests for START pass handlers"
      contains: "test_sk_grammy"
  key_links:
    - from: "ctp/skills/handlers_start.py"
      to: "ctp/skills/engine.py"
      via: "register_skill()"
      pattern: "register_skill"
---

<objective>
Implement 2 START-pass skill handlers: SK_GRAMMY (free build on pass START) and SK_MU_PHEP (bonus increase on pass START). Both trigger ON_PASS_START.

Purpose: These skills activate when player passes the START tile. Same trigger point.

Output: ctp/skills/handlers_start.py with 2 registered handlers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-RESEARCH.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-01-SUMMARY.md

<interfaces>
From ctp/skills/engine.py (Plan 01):
```python
def register_skill(skill_id: str, handler: SkillHandler) -> None: ...
def check_activation(rate: float) -> bool: ...
```

Context dict keys: "board", "players", "passing_bonus" (current START bonus amount)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 2 START-pass handlers</name>
  <files>ctp/skills/handlers_start.py</files>

  <read_first>
    - ctp/skills/engine.py
    - ctp/core/models.py
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_GRAMMY.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_MU_PHEP.md
    - ctp/core/board.py (find empty CITY tiles)
  </read_first>

  <action>
Create `ctp/skills/handlers_start.py`:

**1. handle_sk_grammy(player, ctx) -> list[GameEvent]:**
- Trigger: ON_PASS_START
- Read SK_GRAMMY.md for exact effect: build on random empty CITY tile or upgrade cheapest owned property
- E1: If empty CITY exists -> claim random empty tile at L1 for free
- E2: If no empty CITY but owns property -> upgrade cheapest property +1 level (max L4, D-23)
- Stub AI: no choice needed (automatic selection)
- Return SKILL_ACTIVATED event

**2. handle_sk_mu_phep(player, ctx) -> list[GameEvent]:**
- Trigger: ON_PASS_START
- Read SK_MU_PHEP.md for exact effect: increase START passing bonus by percentage based on rank/star
- Modify ctx["bonus_multiplier"] to increase the passing bonus
- The percentage scales with rank config
- Return SKILL_ACTIVATED event

Register both:
```python
register_skill("SK_GRAMMY", handle_sk_grammy)
register_skill("SK_MU_PHEP", handle_sk_mu_phep)
```
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -c "from ctp.skills.handlers_start import handle_sk_grammy, handle_sk_mu_phep; print('START handlers OK')"</automated>
  </verify>

  <done>2 START-pass handlers registered with free build and bonus increase effects.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for START-pass handlers</name>
  <files>tests/test_handlers_start.py</files>

  <read_first>
    - ctp/skills/handlers_start.py
    - tests/test_skill_system.py
  </read_first>

  <action>
Create `tests/test_handlers_start.py`:

1. **test_sk_grammy_claims_empty_tile** — Empty CITY exists, verify claimed at L1
2. **test_sk_grammy_upgrades_cheapest** — No empty tiles, verify cheapest property upgraded
3. **test_sk_grammy_no_tiles_no_crash** — No empty tiles AND no properties, verify no crash
4. **test_sk_mu_phep_increases_bonus** — Verify ctx["bonus_multiplier"] set > 1.0
5. **test_both_return_events** — Verify both handlers return SKILL_ACTIVATED events
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -m pytest tests/test_handlers_start.py -v</automated>
  </verify>

  <done>All tests pass for START-pass handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| cash/tile mutation | Handlers modify cash bonus and tile ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-09-01 | T (Tampering) | bonus_multiplier | accept | Internal game state |
</threat_model>

<verification>
Both START handlers importable. Tests pass.
</verification>

<success_criteria>
- SK_GRAMMY auto-builds or upgrades on START pass
- SK_MU_PHEP scales passing bonus by rank config percentage
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-09-SUMMARY.md`
</output>

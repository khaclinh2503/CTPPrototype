---
phase: 02.5-skill-pendant-pet-system
plan: 09
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ctp/skills/handlers_start.py
  - tests/test_handlers_start.py
autonomous: true
requirements: [PLAY-02]
must_haves:
  truths:
    - "SK_GRAMMY claims random unowned CITY and builds to L4 when passing START"
    - "SK_MU_PHEP always active (100%), increases passing bonus by value% per rank"
    - "D-47: multiple passing bonuses stack additively"
  artifacts:
    - path: ctp/skills/handlers_start.py
      provides: "SK_GRAMMY, SK_MU_PHEP handlers"
---

<objective>
START-pass skills: SK_GRAMMY, SK_MU_PHEP.

Purpose: Both trigger ON_PASS_START. MuPhep is always-active (no rate check). Bonus stacking per D-47.
Output: 2 handlers + tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/skills/SK_GRAMMY.md
@.planning/phases/02.5-skill-pendant-pet-system/skills/SK_MU_PHEP.md
@ctp/skills/engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SK_GRAMMY + SK_MU_PHEP handlers</name>
  <files>ctp/skills/handlers_start.py, tests/test_handlers_start.py</files>
  <read_first>
    - ctp/skills/engine.py
    - ctp/skills/registry.py
    - ctp/core/models.py
    - ctp/core/board.py
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_GRAMMY.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_MU_PHEP.md
  </read_first>
  <action>
Create `ctp/skills/handlers_start.py`:

**SK_GRAMMY handler (ON_PASS_START):**
- ctx keys: `board`, `players`
- Effect: Choose 1 random unowned CITY tile, set owner + build to L4 (nha cap 3 per D-23)
  ```python
  unowned = [t for t in board.board if t.space_id == SpaceId.CITY and t.owner_id is None]
  if unowned:
      chosen = random.choice(unowned)
      chosen.owner_id = player.player_id
      chosen.building_level = 4  # D-23: nha cap 3 = L4
      player.add_property(chosen.position)
  ```
- Return `{"type": "grammy_claim", "position": chosen.position}` or None if no unowned
- Rate config: A: base=16, chance=1; S: base=22, chance=2

**SK_MU_PHEP handler (ON_PASS_START):**
- **always_active: true** in YAML config — no rate check
- Value (NOT rate): B: base=41, chance=2; A: base=51, chance=3; S: base=66, chance=5
- Value at star: `value = base_value + (star - min_star) * chance`
- Effect: Return `{"type": "passing_bonus_modifier", "percent_increase": value}`
  ```python
  # FSM calculates: bonus = normal_bonus * (1 + value/100)
  # Example S5★: value = 66 + (5-1)*5 = 86 → bonus = 150k * 1.86 = 279k
  ```
- NOTE: MuPhep has config from rank B (not A like most skills)

**SK_SO_10 Effect 2 (also ON_PASS_START):**
- Implemented here as a helper called from SO_10 handler (Plan 11)
- Fixed 50% increase, always active, no rank/star scaling
- Return `{"type": "passing_bonus_modifier", "percent_increase": 50}`

**D-47 Passing Bonus Stacking:**
Create a helper function for FSM integration:
```python
def calc_total_passing_bonus(base_bonus: float, modifiers: list[dict]) -> float:
    """D-47: Additive stacking. MuPhep 86% + SO_10 50% = 150k * (1 + 0.86 + 0.50) = 354k."""
    total_pct = sum(m["percent_increase"] for m in modifiers if m["type"] == "passing_bonus_modifier")
    return base_bonus * (1 + total_pct / 100)
```

Register SK_GRAMMY and SK_MU_PHEP in SKILL_HANDLERS.

**Tests:**
- Grammy: claims random unowned CITY and sets L4
- Grammy: no unowned CITY -> returns None
- Grammy: does not affect Resort tiles
- MuPhep: always active (no random check)
- MuPhep B1★: value=41, S5★: value=86
- MuPhep: returns passing_bonus_modifier with correct percent
- calc_total_passing_bonus: single modifier 86% on 150000 = 279000
- calc_total_passing_bonus: stacking MuPhep 86% + SO_10 50% on 150000 = 354000 (D-47)
  </action>
  <verify>
    <automated>cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_handlers_start.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep "def handle_grammy" ctp/skills/handlers_start.py returns match
    - grep "def handle_mu_phep" ctp/skills/handlers_start.py returns match
    - grep "def calc_total_passing_bonus" ctp/skills/handlers_start.py returns match
    - grep "building_level = 4" ctp/skills/handlers_start.py returns match
    - grep "passing_bonus_modifier" ctp/skills/handlers_start.py returns match
    - grep "always_active" ctp/skills/handlers_start.py or in YAML returns match
    - tests has at least 7 test functions
  </acceptance_criteria>
  <done>Grammy claims + L4. MuPhep always-active bonus. D-47 additive stacking helper. Tests cover stacking scenario.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| N/A | Pure game logic |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-10 | T | bonus calculation | accept | Internal game logic |
</threat_model>

<verification>
tests/test_handlers_start.py all pass.
</verification>

<success_criteria>
- Grammy L4 per D-23. MuPhep always-active. D-47 stacking proven in test.
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-09-SUMMARY.md`
</output>

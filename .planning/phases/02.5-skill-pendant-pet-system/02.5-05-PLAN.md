---
phase: 02.5-skill-pendant-pet-system
plan: 05
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - ctp/skills/handlers_land_same.py
  - tests/test_handlers_land_same.py
autonomous: true
requirements:
  - PLAY-02

must_haves:
  truths:
    - "SK_SUNG_VANG fires when landing on tile where opponent stands, steals property"
    - "SK_LOC_XOAY fires when landing on tile where opponent stands, disables their skills+cards"
    - "SK_TOC_CHIEN fires after travel tile resolution, gives extra movement"
  artifacts:
    - path: "ctp/skills/handlers_land_same.py"
      provides: "SK_SUNG_VANG, SK_LOC_XOAY, SK_TOC_CHIEN handlers"
      contains: "handle_sk_sung_vang"
    - path: "tests/test_handlers_land_same.py"
      provides: "Unit tests for same-tile + travel handlers"
      contains: "test_sk_sung_vang"
  key_links:
    - from: "ctp/skills/handlers_land_same.py"
      to: "ctp/skills/engine.py"
      via: "register_skill()"
      pattern: "register_skill"
---

<objective>
Implement 3 RESOLVE_TILE skill handlers for same-tile and travel triggers: SK_SUNG_VANG (steal property when landing where opponent stands), SK_LOC_XOAY (disable opponent skills/cards), SK_TOC_CHIEN (extra movement after travel).

Purpose: These skills trigger during tile resolution for same-tile encounters and travel tile effects.

Output: ctp/skills/handlers_land_same.py with 3 registered handlers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-RESEARCH.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-01-SUMMARY.md

<interfaces>
From ctp/skills/engine.py (Plan 01):
```python
def register_skill(skill_id: str, handler: SkillHandler) -> None: ...
def check_activation(rate: float) -> bool: ...
```

Context dict keys: "board", "players", "tile", "opponent" (player on same tile)
Player fields: skills_disabled_this_turn, cards_disabled_this_turn
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 3 same-tile + travel RESOLVE_TILE handlers</name>
  <files>ctp/skills/handlers_land_same.py</files>

  <read_first>
    - ctp/skills/engine.py
    - ctp/core/models.py
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_SUNG_VANG.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_LOC_XOAY.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_TOC_CHIEN.md
    - ctp/core/constants.py (calc_invested_build_cost for SK_SUNG_VANG target selection)
  </read_first>

  <action>
Create `ctp/skills/handlers_land_same.py`:

**1. handle_sk_sung_vang(player, ctx) -> list[GameEvent]:**
- Trigger: ON_MOVE_TO_OPPONENT (land on tile where opponent is standing)
- Effect: Steal opponent's most expensive property (D-26: max calc_invested_build_cost). Read SK_SUNG_VANG.md for details.
- Transfer tile ownership: tile.owner_id = player.player_id
- Stub AI: always steal most expensive
- Return SKILL_ACTIVATED event

**2. handle_sk_loc_xoay(player, ctx) -> list[GameEvent]:**
- Trigger: ON_MOVE_TO_OPPONENT (land on tile where opponent stands)
- Effect: Disable opponent's skills AND cards for their NEXT turn. Set opponent.skills_disabled_this_turn = True, opponent.cards_disabled_this_turn = True
- Read SK_LOC_XOAY.md for exact rules
- Return SKILL_ACTIVATED event

**3. handle_sk_toc_chien(player, ctx) -> list[GameEvent]:**
- Trigger: ON_LAND_TRAVEL (after travel tile resolves)
- Effect: After teleport, player gets extra movement (roll again or move N steps). Read SK_TOC_CHIEN.md for exact extra movement rules.
- Set ctx["extra_movement"] = calculated_steps
- Return SKILL_ACTIVATED event

Register all 3:
```python
register_skill("SK_SUNG_VANG", handle_sk_sung_vang)
register_skill("SK_LOC_XOAY", handle_sk_loc_xoay)
register_skill("SK_TOC_CHIEN", handle_sk_toc_chien)
```
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -c "from ctp.skills.handlers_land_same import handle_sk_sung_vang, handle_sk_loc_xoay, handle_sk_toc_chien; print('Same-tile handlers OK')"</automated>
  </verify>

  <done>3 same-tile/travel handlers registered with property theft, disable, and extra movement.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for same-tile + travel handlers</name>
  <files>tests/test_handlers_land_same.py</files>

  <read_first>
    - ctp/skills/handlers_land_same.py
    - tests/test_skill_system.py
  </read_first>

  <action>
Create `tests/test_handlers_land_same.py`:

1. **test_sk_sung_vang_steals_most_expensive** — Setup opponent with 2 properties, verify most expensive one transferred
2. **test_sk_sung_vang_no_property_no_effect** — Opponent owns nothing, verify no crash
3. **test_sk_loc_xoay_disables_skills_and_cards** — Verify opponent flags set True
4. **test_sk_toc_chien_sets_extra_movement** — Verify ctx["extra_movement"] set
5. **test_all_return_events** — All handlers return GameEvent list
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -m pytest tests/test_handlers_land_same.py -v</automated>
  </verify>

  <done>All tests pass for same-tile and travel handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| property transfer | handle_sk_sung_vang transfers tile ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-05-01 | T (Tampering) | tile ownership transfer | accept | Internal game state |
</threat_model>

<verification>
All 3 handlers importable. Tests pass.
</verification>

<success_criteria>
- SK_SUNG_VANG steals most expensive property per D-26
- SK_LOC_XOAY disables opponent skills+cards for next turn
- SK_TOC_CHIEN grants extra movement after travel
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-05-SUMMARY.md`
</output>

---
phase: 02.5-skill-pendant-pet-system
plan: 02
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - ctp/skills/handlers_roll.py
  - tests/test_handlers_roll.py
autonomous: true
requirements:
  - PLAY-02

must_haves:
  truths:
    - "SK_XXCT_1 handler fires ON_ROLL_BEFORE and replaces dice with chosen value"
    - "SK_XE_DO handler fires ON_ROLL_AFTER and replaces move steps (even->even set, odd->odd set)"
    - "SK_MOONWALK handler fires ON_ROLL_AFTER and moves player backward"
    - "SK_XXCT_2 handler fires ON_ROLL_AFTER and replaces dice with any chosen value 2-12"
  artifacts:
    - path: "ctp/skills/handlers_roll.py"
      provides: "4 ROLL-trigger skill handlers"
      contains: "handle_sk_xxct_1"
    - path: "tests/test_handlers_roll.py"
      provides: "Unit tests for all 4 roll handlers"
      contains: "test_sk_xe_do"
  key_links:
    - from: "ctp/skills/handlers_roll.py"
      to: "ctp/skills/engine.py"
      via: "register_skill() for each handler"
      pattern: "register_skill"
---

<objective>
Implement 4 ROLL-trigger skill handlers: SK_XXCT_1 (ON_ROLL_BEFORE), SK_XE_DO, SK_MOONWALK, SK_XXCT_2 (ON_ROLL_AFTER). All share the ROLL phase trigger point.

Purpose: These skills modify dice results or movement direction during the ROLL phase. Grouping by trigger ensures consistent hook point usage.

Output: ctp/skills/handlers_roll.py with 4 registered handlers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-RESEARCH.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-01-SUMMARY.md

<interfaces>
From ctp/skills/engine.py (Plan 01):
```python
def register_skill(skill_id: str, handler: SkillHandler) -> None: ...
def check_activation(rate: float) -> bool: ...
def calc_skill_rate(skill: SkillEntry, player_rank: str, player_star: int) -> float: ...

class SkillEngine:
    def fire(self, trigger: str, player: Player, ctx: dict) -> list[GameEvent]: ...
```

SkillHandler type: `Callable[[Player, dict[str, Any]], list[GameEvent]]`

Context dict keys: "board", "players", "controller", "tile", "dice_result", "dice_pair"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 4 ROLL-trigger skill handlers</name>
  <files>ctp/skills/handlers_roll.py</files>

  <read_first>
    - ctp/skills/engine.py (for register_skill, SkillHandler signature, check_activation)
    - ctp/core/models.py (Player fields)
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_XXCT_1.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_XE_DO.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_MOONWALK.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_XXCT_2.md
  </read_first>

  <action>
Create `ctp/skills/handlers_roll.py` with 4 handler functions:

**1. handle_sk_xxct_1(player, ctx) -> list[GameEvent]:**
- Trigger: ON_ROLL_BEFORE
- Effect: Player chooses dice result before rolling. Stub AI: choose 7 (most common beneficial sum)
- Set ctx["dice_override"] = chosen_value so FSM uses it instead of random roll
- Return SKILL_ACTIVATED event

**2. handle_sk_xe_do(player, ctx) -> list[GameEvent]:**
- Trigger: ON_ROLL_AFTER
- Read ctx["dice_result"]. If even -> player picks from {2,4,6,8,10,12}. If odd -> pick from {1,3,5,7,9,11}
- Stub AI: pick max value (12 if even, 11 if odd)
- Set ctx["move_override"] = chosen_value
- Return SKILL_ACTIVATED event

**3. handle_sk_moonwalk(player, ctx) -> list[GameEvent]:**
- Trigger: ON_ROLL_AFTER
- Effect: Player moves BACKWARD by dice_result steps instead of forward
- Set ctx["move_direction"] = "backward"
- Stub AI: no special logic needed (automatic)
- Return SKILL_ACTIVATED event

**4. handle_sk_xxct_2(player, ctx) -> list[GameEvent]:**
- Trigger: ON_ROLL_AFTER
- Effect: Player chooses ANY value from 2-12 to replace dice result
- Stub AI: choose 12 (max movement)
- Set ctx["move_override"] = chosen_value
- Return SKILL_ACTIVATED event

At module level, register all 4:
```python
register_skill("SK_XXCT_1", handle_sk_xxct_1)
register_skill("SK_XE_DO", handle_sk_xe_do)
register_skill("SK_MOONWALK", handle_sk_moonwalk)
register_skill("SK_XXCT_2", handle_sk_xxct_2)
```

Each handler: read the skill GD file for exact rank config, rate formula, and edge cases. Use check_activation() for rate check inside handler. Follow D-29: stub AI chooses best option by simple heuristic.
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -c "from ctp.skills.handlers_roll import handle_sk_xxct_1, handle_sk_xe_do, handle_sk_moonwalk, handle_sk_xxct_2; print('All 4 roll handlers imported OK')"</automated>
  </verify>

  <done>4 ROLL-trigger handlers registered, each modifying dice/movement via ctx overrides. Stub AI picks optimal values per D-29.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for ROLL handlers</name>
  <files>tests/test_handlers_roll.py</files>

  <read_first>
    - ctp/skills/handlers_roll.py (just created)
    - tests/test_skill_system.py (test patterns from Plan 01)
  </read_first>

  <action>
Create `tests/test_handlers_roll.py` with tests:

1. **test_sk_xxct_1_sets_dice_override** — Call handle_sk_xxct_1 with mock player/ctx, verify ctx["dice_override"] is set
2. **test_sk_xe_do_even_picks_even** — dice_result=6 (even), verify ctx["move_override"] in {2,4,6,8,10,12}
3. **test_sk_xe_do_odd_picks_odd** — dice_result=7 (odd), verify ctx["move_override"] in {1,3,5,7,9,11}
4. **test_sk_moonwalk_sets_backward** — verify ctx["move_direction"] == "backward"
5. **test_sk_xxct_2_sets_move_override** — verify ctx["move_override"] in range(2,13)
6. **test_all_handlers_return_events** — each handler returns list[GameEvent] with SKILL_ACTIVATED

Use pytest fixtures for Player with rank="R", star=5 (testing defaults).
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -m pytest tests/test_handlers_roll.py -v</automated>
  </verify>

  <done>All 6+ tests pass. Each handler verified for correct ctx mutation and event return.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ctx dict | Handlers receive ctx from FSM — trusted internal boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-02-01 | T (Tampering) | ctx["move_override"] | accept | Internal game loop, no external input |
</threat_model>

<verification>
All 4 roll handlers importable and registered. Unit tests pass.
</verification>

<success_criteria>
- 4 handler functions in handlers_roll.py
- All registered via register_skill()
- Unit tests cover each handler's ctx mutation
- Stub AI picks optimal values per D-29
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-02-SUMMARY.md`
</output>

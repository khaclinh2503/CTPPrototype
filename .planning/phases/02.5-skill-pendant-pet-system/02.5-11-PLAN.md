---
phase: 02.5-skill-pendant-pet-system
plan: 11
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - ctp/skills/handlers_multi.py
  - tests/test_handlers_multi.py
autonomous: true
requirements:
  - PLAY-02

must_haves:
  truths:
    - "SK_GAY_NHU_Y E1 fires reactively when opponent reaches Resort, follows them there"
    - "SK_GAY_NHU_Y E2 fires ON_UPGRADE, upgrades to L5 + 70% chance move to Resort"
    - "SK_HO_DIEP E1 fires reactively when opponent uses movement skill, sends them to prison"
    - "SK_HO_DIEP E2 fires ON_UPGRADE with color pair, moves to nearest empty CITY same edge"
    - "SK_SO_10 E1 fires ON_LAND_TRAVEL, teleport to any chosen tile"
    - "SK_SO_10 E2 always active, increases START passing bonus by 50%"
  artifacts:
    - path: "ctp/skills/handlers_multi.py"
      provides: "SK_GAY_NHU_Y, SK_HO_DIEP, SK_SO_10 handlers"
      contains: "handle_sk_gay_nhu_y"
    - path: "tests/test_handlers_multi.py"
      provides: "Unit tests for multi-trigger handlers"
      contains: "test_sk_gay_nhu_y"
  key_links:
    - from: "ctp/skills/handlers_multi.py"
      to: "ctp/skills/engine.py"
      via: "register_skill()"
      pattern: "register_skill"
---

<objective>
Implement 3 complex multi-trigger skill handlers: SK_GAY_NHU_Y (2 triggers: reactive resort follow + upgrade to L5), SK_HO_DIEP (2 triggers: reactive prison throw + upgrade with color pair move), SK_SO_10 (2 effects: travel teleport + always-on START bonus).

Purpose: These skills each have 2 distinct triggers or effects, making them the most complex handlers. Grouped together for consistency in multi-trigger handling.

Output: ctp/skills/handlers_multi.py with 3 registered handlers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-RESEARCH.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-01-SUMMARY.md

<interfaces>
From ctp/skills/engine.py (Plan 01):
```python
def register_skill(skill_id: str, handler: SkillHandler) -> None: ...
def check_activation(rate: float) -> bool: ...
```

Context dict keys: "board", "players", "tile", "opponent", "trigger" (which trigger fired)
D-27: "same edge" = same 1 of 4 edges of diamond board (32 tiles)
D-24: "Bieu Tuong" = building_level = 5
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 3 multi-trigger skill handlers</name>
  <files>ctp/skills/handlers_multi.py</files>

  <read_first>
    - ctp/skills/engine.py
    - ctp/core/models.py
    - ctp/core/board.py
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_GAY_NHU_Y.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_HO_DIEP.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_SO_10.md
    - ctp/config/Board.json (for edge/side calculation per D-27)
  </read_first>

  <action>
Create `ctp/skills/handlers_multi.py`:

**Helper: _get_board_edge(position) -> int:**
Return which edge (0-3) a position belongs to on the 32-tile diamond board. Tiles 1-8 = edge 0, 9-16 = edge 1, 17-24 = edge 2, 25-32 = edge 3 (verify exact mapping from Board.json).

**1. handle_sk_gay_nhu_y(player, ctx) -> list[GameEvent]:**
Per SK_GAY_NHU_Y.md — 2 effects based on trigger:
- E1 (ON_OPPONENT_TRAVEL / reactive): When opponent moves to Resort tile, player follows to same Resort. player.move_to(resort_position). Tile effect (toll) resolves for player.
- E2 (ON_UPGRADE): Upgrade to L5 (Bieu Tuong). Then secondary 70% fixed rate: move to Resort tile. This is the TEDDY pattern with 70% instead of 60%.
- Branch on ctx["trigger"] to determine which effect

**2. handle_sk_ho_diep(player, ctx) -> list[GameEvent]:**
Per SK_HO_DIEP.md — 2 effects:
- E1 (ON_OPPONENT_MOVE_SKILL / reactive): When opponent activates a movement skill, send opponent to prison (opponent.enter_prison()). Check rate1.
- E2 (ON_UPGRADE): When upgrading AND player owns a color pair (>=2 CITY same color), move to nearest empty CITY on same edge (D-27). Check rate2. If no empty CITY on same edge, fail silently. Tile effect does NOT trigger at destination.
- Branch on ctx["trigger"]

**3. handle_sk_so_10(player, ctx) -> list[GameEvent]:**
Per SK_SO_10.md — 2 effects:
- E1 (ON_LAND_TRAVEL): After travel resolves, teleport to ANY chosen tile. Stub AI: choose most valuable own property or nearest empty CITY (D-29). Tile effect does NOT trigger at destination.
- E2 (always active, 100%): Increase START passing bonus by 50%. Modify ctx["bonus_multiplier"] += 0.5. This fires on ON_PASS_START with no rate check needed.
- Different triggers for E1 and E2

Register all 3:
```python
register_skill("SK_GAY_NHU_Y", handle_sk_gay_nhu_y)
register_skill("SK_HO_DIEP", handle_sk_ho_diep)
register_skill("SK_SO_10", handle_sk_so_10)
```
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -c "from ctp.skills.handlers_multi import handle_sk_gay_nhu_y, handle_sk_ho_diep, handle_sk_so_10; print('Multi-trigger handlers OK')"</automated>
  </verify>

  <done>3 multi-trigger handlers registered, each branching on trigger type for dual effects.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for multi-trigger handlers</name>
  <files>tests/test_handlers_multi.py</files>

  <read_first>
    - ctp/skills/handlers_multi.py
    - tests/test_skill_system.py
    - ctp/config/Board.json (edge mapping verification)
  </read_first>

  <action>
Create `tests/test_handlers_multi.py`:

1. **test_sk_gay_nhu_y_e1_follows_to_resort** — Trigger ON_OPPONENT_TRAVEL, verify player moved to resort pos
2. **test_sk_gay_nhu_y_e2_upgrades_to_l5** — Trigger ON_UPGRADE, verify tile.building_level = 5
3. **test_sk_gay_nhu_y_e2_secondary_70** — Mock random for 70% travel check
4. **test_sk_ho_diep_e1_sends_to_prison** — Trigger ON_OPPONENT_MOVE_SKILL, verify opponent in prison
5. **test_sk_ho_diep_e2_moves_to_empty_same_edge** — Trigger ON_UPGRADE with color pair, verify movement to same edge
6. **test_sk_ho_diep_e2_no_empty_same_edge** — No empty tile on edge, verify no crash
7. **test_sk_so_10_e1_teleport_any_tile** — Trigger ON_LAND_TRAVEL, verify position change
8. **test_sk_so_10_e2_bonus_50_percent** — Trigger ON_PASS_START, verify bonus_multiplier += 0.5
9. **test_board_edge_helper** — Verify _get_board_edge returns correct edge for known positions
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -m pytest tests/test_handlers_multi.py -v</automated>
  </verify>

  <done>All tests pass for multi-trigger handlers covering both effects per skill.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player movement | Handlers move players to arbitrary positions |
| prison entry | SK_HO_DIEP sends opponents to prison reactively |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-11-01 | T (Tampering) | player position | accept | Internal game loop |
</threat_model>

<verification>
All 3 multi-trigger handlers importable. Tests cover both effects per skill. Edge helper tested.
</verification>

<success_criteria>
- Each handler correctly branches on trigger type
- SK_GAY_NHU_Y: resort follow + Teddy-like L5 upgrade with 70% secondary
- SK_HO_DIEP: prison throw + color-pair edge movement
- SK_SO_10: travel teleport + always-on 50% START bonus
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-11-SUMMARY.md`
</output>

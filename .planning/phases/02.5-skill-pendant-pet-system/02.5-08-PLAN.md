---
phase: 02.5-skill-pendant-pet-system
plan: 08
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - ctp/skills/handlers_hybrid.py
  - tests/test_handlers_hybrid.py
autonomous: true
requirements:
  - PLAY-02

must_haves:
  truths:
    - "SK_AO_ANH fires on UPGRADE or landing at own landmark, places illusion trap on board"
    - "SK_BIEN_CAM fires on UPGRADE to L5 or landing at own L5, places stop sign on board"
  artifacts:
    - path: "ctp/skills/handlers_hybrid.py"
      provides: "SK_AO_ANH, SK_BIEN_CAM handlers"
      contains: "handle_sk_ao_anh"
    - path: "tests/test_handlers_hybrid.py"
      provides: "Unit tests for hybrid upgrade+land handlers"
      contains: "test_sk_ao_anh"
  key_links:
    - from: "ctp/skills/handlers_hybrid.py"
      to: "ctp/core/board.py"
      via: "Sets board.illusion_position and board.stop_sign_position"
      pattern: "illusion_position|stop_sign_position"
---

<objective>
Implement 2 hybrid skill handlers that trigger on both UPGRADE and LAND at own landmark: SK_AO_ANH (illusion trap) and SK_BIEN_CAM (stop sign). Both modify Board state fields.

Purpose: These skills place persistent board traps that affect opponent movement (D-13/D-14). Grouped because both are hybrid upgrade+land triggers modifying Board state.

Output: ctp/skills/handlers_hybrid.py with 2 registered handlers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-RESEARCH.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-01-SUMMARY.md

<interfaces>
From ctp/core/board.py (Plan 01 additions):
```python
self.illusion_position: int | None = None   # SK_AO_ANH
self.stop_sign_position: int | None = None  # SK_BIEN_CAM
```

Board state rules:
- Only 1 illusion on map at a time (new replaces old regardless of owner)
- Only 1 stop sign on map at a time
- Both checked per-step in MOVE phase (FSM plan will wire this)
- Illusion: opponent stops mid-path, tile effect resolves, illusion removed
- Stop sign: opponent stops, tile effect resolves, stop sign removed
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 2 hybrid upgrade+land handlers</name>
  <files>ctp/skills/handlers_hybrid.py</files>

  <read_first>
    - ctp/skills/engine.py
    - ctp/core/models.py
    - ctp/core/board.py (illusion_position, stop_sign_position)
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_AO_ANH.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_BIEN_CAM.md
  </read_first>

  <action>
Create `ctp/skills/handlers_hybrid.py`:

**1. handle_sk_ao_anh(player, ctx) -> list[GameEvent]:**
- Triggers: ON_UPGRADE (any upgrade) OR ON_LAND when tile is own L5 landmark
- Per SK_AO_ANH.md: place illusion at current tile position
- Set ctx["board"].illusion_position = ctx["tile"].position
- One illusion globally — new placement overwrites old (any owner)
- Illusion does NOT affect the player who placed it
- Return SKILL_ACTIVATED event

**2. handle_sk_bien_cam(player, ctx) -> list[GameEvent]:**
- Triggers: ON_UPGRADE to L5 (Bieu Tuong) OR ON_LAND at own L5
- Per SK_BIEN_CAM.md: place stop sign at a chosen position
- Set ctx["board"].stop_sign_position = chosen_position
- One stop sign globally — new placement overwrites old
- Stub AI: place at opponent's most valuable property position
- Return SKILL_ACTIVATED event

IMPORTANT: Both skills have DUAL triggers — they must be registered for both ON_UPGRADE and ON_LAND. The SkillEngine should dispatch to the same handler for both triggers. Register with a wrapper or register twice:
```python
register_skill("SK_AO_ANH", handle_sk_ao_anh)  # engine checks trigger from YAML config
register_skill("SK_BIEN_CAM", handle_sk_bien_cam)
```

Since each skill has a single trigger in YAML, these skills need `trigger: "ON_UPGRADE"` in YAML AND a secondary check inside the handler for ON_LAND at own landmark. OR: register as multi-trigger in engine. Follow the pattern established by Plan 01's SkillEngine for multi-trigger skills.
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -c "from ctp.skills.handlers_hybrid import handle_sk_ao_anh, handle_sk_bien_cam; print('Hybrid handlers OK')"</automated>
  </verify>

  <done>2 hybrid handlers registered, both modifying Board illusion/stop_sign fields.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for hybrid handlers</name>
  <files>tests/test_handlers_hybrid.py</files>

  <read_first>
    - ctp/skills/handlers_hybrid.py
    - tests/test_skill_system.py
  </read_first>

  <action>
Create `tests/test_handlers_hybrid.py`:

1. **test_sk_ao_anh_places_illusion** — Verify board.illusion_position set to tile position
2. **test_sk_ao_anh_overwrites_existing** — Old illusion at pos 5, new at pos 10, verify pos=10
3. **test_sk_ao_anh_triggers_on_upgrade** — Verify activation on upgrade trigger
4. **test_sk_ao_anh_triggers_on_land_own_l5** — Verify activation when landing at own L5
5. **test_sk_bien_cam_places_stop_sign** — Verify board.stop_sign_position set
6. **test_sk_bien_cam_overwrites_existing** — Verify replacement behavior
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -m pytest tests/test_handlers_hybrid.py -v</automated>
  </verify>

  <done>All tests pass for illusion and stop sign placement including overwrite behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| board state | Handlers modify global board trap positions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-08-01 | T (Tampering) | board trap positions | accept | Internal game state |
</threat_model>

<verification>
Both hybrid handlers importable. Board state fields modified correctly. Tests pass.
</verification>

<success_criteria>
- SK_AO_ANH places illusion trap, globally unique, overwrites previous
- SK_BIEN_CAM places stop sign, globally unique, overwrites previous
- Both trigger on upgrade AND land at own landmark
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-08-SUMMARY.md`
</output>

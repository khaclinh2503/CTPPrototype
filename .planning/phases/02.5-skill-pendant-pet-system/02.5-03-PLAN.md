---
phase: 02.5-skill-pendant-pet-system
plan: 03
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - ctp/skills/handlers_move.py
  - tests/test_handlers_move.py
autonomous: true
requirements:
  - PLAY-02

must_haves:
  truths:
    - "SK_CAM_CO fires ON_MOVE_PASS when passing CITY with owner_id=None, claims it free with decay rate"
    - "SK_PHA_HUY fires ON_MOVE_PASS when passing opponent property, destroys building level"
  artifacts:
    - path: "ctp/skills/handlers_move.py"
      provides: "SK_CAM_CO, SK_PHA_HUY handlers"
      contains: "handle_sk_cam_co"
    - path: "tests/test_handlers_move.py"
      provides: "Unit tests for move handlers"
      contains: "test_sk_cam_co"
  key_links:
    - from: "ctp/skills/handlers_move.py"
      to: "ctp/skills/engine.py"
      via: "register_skill()"
      pattern: "register_skill"
---

<objective>
Implement 2 MOVE-trigger skill handlers: SK_CAM_CO (claim empty CITY while passing) and SK_PHA_HUY (destroy opponent building while passing). Both trigger ON_MOVE_PASS.

Purpose: These skills activate during movement phase as player passes through tiles, sharing the same hook point.

Output: ctp/skills/handlers_move.py with 2 registered handlers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-RESEARCH.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-01-SUMMARY.md

<interfaces>
From ctp/skills/engine.py (Plan 01):
```python
def register_skill(skill_id: str, handler: SkillHandler) -> None: ...
def check_activation(rate: float) -> bool: ...
```

Context dict keys for MOVE: "board", "players", "tile" (tile being passed), "player"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 2 MOVE-trigger skill handlers</name>
  <files>ctp/skills/handlers_move.py</files>

  <read_first>
    - ctp/skills/engine.py
    - ctp/core/models.py (Player fields: cam_co_decay_index, cam_co_current_rate)
    - ctp/core/board.py (Board, Tile structure)
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_CAM_CO.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_PHA_HUY.md
    - ctp/core/constants.py (calc_invested_build_cost)
  </read_first>

  <action>
Create `ctp/skills/handlers_move.py`:

**1. handle_sk_cam_co(player, ctx) -> list[GameEvent]:**
- Trigger: ON_MOVE_PASS
- Check tile from ctx["tile"]: must be CITY with owner_id is None (D-25: empty land)
- Rate uses decay mechanic: first activation in turn uses base rate, each subsequent activation in same turn decays. Track via player.cam_co_decay_index and player.cam_co_current_rate
- Read SK_CAM_CO.md for exact decay formula
- If active: claim tile for player (tile.owner_id = player.player_id, tile.building_level = 1)
- Return SKILL_ACTIVATED event with tile position

**2. handle_sk_pha_huy(player, ctx) -> list[GameEvent]:**
- Trigger: ON_MOVE_PASS
- Check tile from ctx["tile"]: must be CITY with owner_id != None and owner_id != player.player_id (opponent property)
- If active: destroy building — reduce building_level by 1 (min 1). If building_level was 1 -> remove ownership (tile.owner_id = None, tile.building_level = 0)
- Read SK_PHA_HUY.md for exact destruction rules and compensation formula
- Return SKILL_ACTIVATED event

Register both:
```python
register_skill("SK_CAM_CO", handle_sk_cam_co)
register_skill("SK_PHA_HUY", handle_sk_pha_huy)
```

IMPORTANT: cam_co_decay_index and cam_co_current_rate must be reset at END_TURN (this will be wired in the FSM plan).
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -c "from ctp.skills.handlers_move import handle_sk_cam_co, handle_sk_pha_huy; print('Move handlers imported OK')"</automated>
  </verify>

  <done>2 MOVE-trigger handlers registered with correct tile ownership/destruction logic.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for MOVE handlers</name>
  <files>tests/test_handlers_move.py</files>

  <read_first>
    - ctp/skills/handlers_move.py
    - tests/test_skill_system.py (fixture patterns)
  </read_first>

  <action>
Create `tests/test_handlers_move.py`:

1. **test_sk_cam_co_claims_empty_city** — Pass empty CITY tile, verify tile.owner_id set to player
2. **test_sk_cam_co_ignores_owned_tile** — Pass owned tile, verify no change
3. **test_sk_cam_co_decay_rate** — Multiple activations in one turn, verify rate decreases
4. **test_sk_pha_huy_reduces_level** — Opponent tile L3, verify building_level reduced by 1
5. **test_sk_pha_huy_removes_ownership_at_l1** — Opponent tile L1, verify owner_id becomes None
6. **test_sk_pha_huy_ignores_own_property** — Own tile, verify no change
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -m pytest tests/test_handlers_move.py -v</automated>
  </verify>

  <done>All tests pass covering claim, decay, destruction, and edge cases.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tile mutation | Handlers modify tile ownership — internal game state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-03-01 | T (Tampering) | tile.owner_id mutation | accept | Internal game loop only |
</threat_model>

<verification>
Both move handlers importable and registered. Tests pass.
</verification>

<success_criteria>
- SK_CAM_CO claims empty tiles with decay rate mechanic
- SK_PHA_HUY destroys opponent buildings correctly
- Edge cases handled (own property, non-CITY tiles ignored)
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-03-SUMMARY.md`
</output>

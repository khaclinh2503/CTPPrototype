---
phase: 02.5-skill-pendant-pet-system
plan: 06
type: execute
wave: 2
depends_on: ["02.5-01"]
files_modified:
  - ctp/skills/handlers_prison.py
  - tests/test_handlers_prison.py
autonomous: true
requirements:
  - PLAY-02

must_haves:
  truths:
    - "SK_JOKER fires ON_ENTER_PRISON, gives reward or deferred reward via joker_pending"
    - "SK_HQXX fires on landing any tile, resets consecutive_doubles to avoid prison"
    - "SK_LAU_DAI_TINH_AI fires when landing at own L5 landmark, pulls random opponent"
  artifacts:
    - path: "ctp/skills/handlers_prison.py"
      provides: "SK_JOKER, SK_HQXX, SK_LAU_DAI_TINH_AI handlers"
      contains: "handle_sk_joker"
    - path: "tests/test_handlers_prison.py"
      provides: "Unit tests for prison + landmark handlers"
      contains: "test_sk_joker"
  key_links:
    - from: "ctp/skills/handlers_prison.py"
      to: "ctp/skills/engine.py"
      via: "register_skill()"
      pattern: "register_skill"
---

<objective>
Implement 3 skill handlers for prison and landmark triggers: SK_JOKER (prison entry reward), SK_HQXX (doubles reset to avoid prison), SK_LAU_DAI_TINH_AI (pull opponent to own landmark).

Purpose: SK_JOKER and SK_HQXX relate to prison mechanics. SK_LAU_DAI_TINH_AI lands at own L5 (landmark resolve). Grouped for shared prison/landmark context.

Output: ctp/skills/handlers_prison.py with 3 registered handlers + unit tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-RESEARCH.md
@.planning/phases/02.5-skill-pendant-pet-system/02.5-01-SUMMARY.md

<interfaces>
From ctp/skills/engine.py (Plan 01):
```python
def register_skill(skill_id: str, handler: SkillHandler) -> None: ...
def check_activation(rate: float) -> bool: ...
```

Context dict keys: "board", "players", "tile", "controller"
Player fields: joker_pending, consecutive_doubles
Player methods: enter_prison(), exit_prison(), move_to()
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement 3 prison + landmark handlers</name>
  <files>ctp/skills/handlers_prison.py</files>

  <read_first>
    - ctp/skills/engine.py
    - ctp/core/models.py (Player: joker_pending, consecutive_doubles, enter_prison, exit_prison)
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_JOKER.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_HQXX.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_LAU_DAI_TINH_AI.md
    - ctp/tiles/prison.py (prison mechanics reference)
  </read_first>

  <action>
Create `ctp/skills/handlers_prison.py`:

**1. handle_sk_joker(player, ctx) -> list[GameEvent]:**
- Trigger: ON_ENTER_PRISON
- Read SK_JOKER.md for 2 cases:
  - TH1: Player enters prison during their own turn -> immediate reward (cash bonus or exit)
  - TH2: Player enters prison outside their turn (opponent skill) -> set player.joker_pending = True, reward given at start of player's next ROLL phase
- Return SKILL_ACTIVATED event

**2. handle_sk_hqxx(player, ctx) -> list[GameEvent]:**
- Trigger: ON_LAND (landing at any tile — checks doubles streak)
- Read SK_HQXX.md for exact effect
- Effect: Reset player.consecutive_doubles = 0 to prevent triple-doubles prison entry
- This skill fires broadly but only has effect when consecutive_doubles > 0
- Return SKILL_ACTIVATED event

**3. handle_sk_lau_dai_tinh_ai(player, ctx) -> list[GameEvent]:**
- Trigger: ON_LAND (when player lands at their OWN L5/Bieu Tuong)
- Check: ctx["tile"].owner_id == player.player_id AND ctx["tile"].building_level == 5 (D-24)
- If active: pick random non-bankrupt opponent, move them to player's position. Toll resolves for pulled opponent.
- Stub AI: no choice needed (random opponent)
- Return SKILL_ACTIVATED event with target opponent info

Register all 3:
```python
register_skill("SK_JOKER", handle_sk_joker)
register_skill("SK_HQXX", handle_sk_hqxx)
register_skill("SK_LAU_DAI_TINH_AI", handle_sk_lau_dai_tinh_ai)
```
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -c "from ctp.skills.handlers_prison import handle_sk_joker, handle_sk_hqxx, handle_sk_lau_dai_tinh_ai; print('Prison+landmark handlers OK')"</automated>
  </verify>

  <done>3 handlers registered for prison entry reward, doubles reset, and landmark pull.</done>
</task>

<task type="auto">
  <name>Task 2: Unit tests for prison + landmark handlers</name>
  <files>tests/test_handlers_prison.py</files>

  <read_first>
    - ctp/skills/handlers_prison.py
    - tests/test_skill_system.py
  </read_first>

  <action>
Create `tests/test_handlers_prison.py`:

1. **test_sk_joker_own_turn_immediate_reward** — Player enters prison in own turn, verify reward
2. **test_sk_joker_outside_turn_sets_pending** — Outside turn, verify joker_pending = True
3. **test_sk_hqxx_resets_doubles** — Set consecutive_doubles=2, verify reset to 0
4. **test_sk_hqxx_no_effect_when_no_doubles** — consecutive_doubles=0, verify no-op
5. **test_sk_lau_dai_tinh_ai_pulls_opponent** — Land at own L5, verify opponent moved
6. **test_sk_lau_dai_tinh_ai_not_own_l5** — Land at opponent L5, verify no activation
  </action>

  <verify>
    <automated>cd /Users/lap60800/Documents/workspace/CTPPrototype && python -m pytest tests/test_handlers_prison.py -v</automated>
  </verify>

  <done>All tests pass for prison and landmark handlers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| player state | Handlers modify joker_pending, consecutive_doubles, position |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-06-01 | T (Tampering) | joker_pending flag | accept | Internal game state |
</threat_model>

<verification>
All 3 handlers importable. Tests pass.
</verification>

<success_criteria>
- SK_JOKER handles both in-turn and out-of-turn prison entry per GD spec
- SK_HQXX resets consecutive_doubles to prevent forced prison
- SK_LAU_DAI_TINH_AI pulls opponent to own L5 with toll resolution
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-06-SUMMARY.md`
</output>

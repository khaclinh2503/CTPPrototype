---
phase: 02.5-skill-pendant-pet-system
plan: 06
type: execute
wave: 2
depends_on: [01]
files_modified:
  - ctp/skills/handlers_prison.py
  - tests/test_handlers_prison.py
autonomous: true
requirements: [PLAY-02]
must_haves:
  truths:
    - "SK_JOKER TH1: exit prison + choose unowned CITY + extra roll (own turn)"
    - "SK_JOKER TH2: exit prison immediately, pending reward next turn (other turn)"
    - "SK_HQXX grants extra roll + pushes opponents on same side to prison + resets doubles"
    - "SK_LAU_DAI_TINH_AI pulls random opponent to player's L5"
  artifacts:
    - path: ctp/skills/handlers_prison.py
      provides: "SK_JOKER, SK_HQXX, SK_LAU_DAI_TINH_AI handlers"
---

<objective>
Prison + landmark skills: SK_JOKER, SK_HQXX, SK_LAU_DAI_TINH_AI.

Purpose: Joker handles prison escape, HQXX is a powerful combo skill on any tile, LDTA pulls opponents to L5 landmarks.
Output: 3 handlers + tests.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.5-skill-pendant-pet-system/02.5-CONTEXT.md
@.planning/phases/02.5-skill-pendant-pet-system/skills/SK_JOKER.md
@.planning/phases/02.5-skill-pendant-pet-system/skills/SK_HQXX.md
@.planning/phases/02.5-skill-pendant-pet-system/skills/SK_LAU_DAI_TINH_AI.md
@ctp/skills/engine.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: SK_JOKER, SK_HQXX, SK_LAU_DAI_TINH_AI handlers</name>
  <files>ctp/skills/handlers_prison.py, tests/test_handlers_prison.py</files>
  <read_first>
    - ctp/skills/engine.py
    - ctp/skills/registry.py
    - ctp/core/models.py
    - ctp/core/board.py
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_JOKER.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_HQXX.md
    - .planning/phases/02.5-skill-pendant-pet-system/skills/SK_LAU_DAI_TINH_AI.md
  </read_first>
  <action>
Create `ctp/skills/handlers_prison.py`:

**SK_JOKER handler (ON_ENTER_PRISON):**
- ctx keys: `board`, `players`, `is_player_turn`, `choose_fn`
- Rate config: A: base=50, chance=2; S: base=60, chance=3

**TH1 (is_player_turn=True):** All 3 effects immediately:
1. `player.exit_prison()`
2. Choose unowned CITY tile (Skill Walk immune movement per D-54):
   ```python
   unowned = [t for t in board.tiles if t.space_id == SpaceId.CITY and t.owner_id is None]
   if unowned:
       chosen = choose_fn(unowned)  # stub: nearest to current position
       player.move_to(chosen.position)
       # tile effect does NOT trigger
   ```
3. Return `{"type": "extra_roll"}` for FSM to grant extra turn

**TH2 (is_player_turn=False — pushed to prison by opponent skill/card):**
1. `player.exit_prison()` — immediate
2. `player.joker_pending = True` — effects 2+3 deferred to next turn
3. At player's next turn start (FSM checks joker_pending):
   - Choose unowned CITY + move there
   - Grant extra roll
   - `player.joker_pending = False`

**SK_HQXX handler (ON_LAND — when landing on any tile):**
- ctx keys: `tile`, `board`, `players`
- Effect (all 3 simultaneous):
  1. Grant extra roll: return `{"type": "extra_roll"}`
  2. Push opponents on same board side to prison:
     ```python
     side_positions = board.get_row_non_corner_positions(player.position)
     for opp in players:
         if opp.player_id != player.player_id and not opp.is_bankrupt:
             if opp.position in side_positions:
                 opp.enter_prison()
     ```
  3. Reset doubles: `player.consecutive_doubles = 0`
- Rate config: A: base=20, chance=1; S: base=25, chance=2

**SK_LAU_DAI_TINH_AI handler (ON_LAND — when landing on OWN L5):**
- ctx keys: `tile`, `board`, `players`
- Check: `tile.owner_id == player.player_id and tile.building_level == 5`
- Effect: choose 1 random non-bankrupt opponent, Teleport to player's current L5 position
  - D-48: skip if opponent already at this position
  - Tile effect (toll) triggers normally for pulled opponent
- Rate config: A: base=38, chance=2; S: base=48, chance=3

Register all 3 in SKILL_HANDLERS.

**Tests:**
- Joker TH1: exit prison + move to unowned CITY + extra_roll
- Joker TH2: exit prison immediately, joker_pending=True
- Joker: no unowned CITY -> skip move, still exit+extra_roll
- HQXX: extra_roll + push opponents on same side to prison
- HQXX: no opponents on same side -> only extra_roll + reset doubles
- HQXX: consecutive_doubles reset to 0
- LDTA: pulls random opponent to player's L5
- LDTA: skip opponent already at position (D-48)
- LDTA: does not fire at non-L5 tile
  </action>
  <verify>
    <automated>cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_handlers_prison.py -x -v</automated>
  </verify>
  <acceptance_criteria>
    - grep "def handle_joker" ctp/skills/handlers_prison.py returns match
    - grep "def handle_hqxx" ctp/skills/handlers_prison.py returns match
    - grep "def handle_lau_dai_tinh_ai" ctp/skills/handlers_prison.py returns match
    - grep "joker_pending" ctp/skills/handlers_prison.py returns match
    - grep "exit_prison" ctp/skills/handlers_prison.py returns match
    - grep "enter_prison" ctp/skills/handlers_prison.py returns match
    - grep "consecutive_doubles.*=.*0" ctp/skills/handlers_prison.py returns match
    - grep "get_row_non_corner_positions" ctp/skills/handlers_prison.py returns match
    - tests has at least 8 test functions
  </acceptance_criteria>
  <done>Joker TH1/TH2 both handled. HQXX 3 effects work. LDTA pulls opponent to L5. D-48 skip-already-there enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| N/A | Pure game logic |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-02.5-07 | D | enter_prison loop | mitigate | Joker checks prison_turns before exit to avoid double-exit |
</threat_model>

<verification>
tests/test_handlers_prison.py all pass.
</verification>

<success_criteria>
- Joker TH1/TH2 correctly differentiated by is_player_turn
- joker_pending flag defers effects to next turn
- HQXX pushes same-side opponents + extra_roll + reset doubles
- LDTA pulls random opponent to L5, D-48 enforced
</success_criteria>

<output>
After completion, create `.planning/phases/02.5-skill-pendant-pet-system/02.5-06-SUMMARY.md`
</output>

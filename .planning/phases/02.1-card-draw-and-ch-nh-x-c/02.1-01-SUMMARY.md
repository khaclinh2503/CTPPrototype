---
phase: 02.1-card-draw-and-ch-nh-x-c
plan: "01"
subsystem: core-models
tags: [models, events, board, card-draw, can-luc, tdd]
dependency_graph:
  requires: []
  provides: [Player.held_card, Player.accuracy_rate, Player.virus_turns, Player.double_toll_turns, EventType.CARD_EFFECT_*, Board.map_id, Board.find_nearest_tile_by_space_id]
  affects: [ctp/tiles/fortune.py, ctp/tiles/land.py]
tech_stack:
  added: []
  patterns: [dataclass-fields, enum-auto, forward-search-wraparound]
key_files:
  created:
    - tests/test_models.py
    - tests/test_events.py
    - tests/test_board.py
  modified:
    - ctp/core/models.py
    - ctp/core/events.py
    - ctp/core/board.py
decisions:
  - "Board nhận thêm travel_config=None param cùng lúc với map_id=1 để sẵn sàng cho Plan 02 và 03"
  - "find_nearest_tile_by_space_id() wrap-around 32 bước, không tính ô xuất phát"
metrics:
  duration: "~15 min"
  completed_date: "2026-04-03"
  tasks_completed: 3
  files_modified: 6
---

# Phase 02.1 Plan 01: Building Blocks cho Card Draw System — Summary

**One-liner:** 4 Player fields (held_card/accuracy_rate/virus_turns/double_toll_turns) + 20 EventType card effect constants + Board.map_id và find_nearest_tile_by_space_id() — tất cả backward compatible, 13 tests mới pass.

## Tasks Completed

| Task | Name | Commit | Files |
|------|------|--------|-------|
| 1 | Thêm 4 fields mới vào Player dataclass | 78168b6 | ctp/core/models.py, tests/test_models.py |
| 2 | Thêm 20 EventType constants cho card effects | b27797e | ctp/core/events.py, tests/test_events.py |
| 3 | Thêm map_id field và find_nearest_tile_by_space_id() vào Board | 766d32e | ctp/core/board.py, tests/test_board.py |

## What Was Built

### Task 1: Player fields (ctp/core/models.py, lines 31-34 added)

Thêm 4 fields sau `turns_taken: int = 0`:

```python
held_card: str | None = None          # card_id đang giữ (ví dụ "IT_CA_3"), per D-09
accuracy_rate: int = 15               # căn lực base rate (%), per D-10
virus_turns: int = 0                  # EF_7/8 debuff rounds remaining, per D-11
double_toll_turns: int = 0            # EF_16 self-debuff rounds remaining, per D-12
```

### Task 2: EventType constants (ctp/core/events.py, lines 47-66 added)

20 card effect event types sau `WATER_SLIDE_PUSHED`:
- CARD_EFFECT_ANGEL, DISCOUNT_TOLL, SHIELD_BLOCKED, ESCAPE_USED, PINWHEEL_BYPASS
- CARD_EFFECT_FORCE_SELL, SWAP_CITY, DOWNGRADE, VIRUS
- CARD_EFFECT_GO_TO_START/PRISON/FESTIVAL/FESTIVAL_TILE/TRAVEL/TAX/GOD
- CARD_EFFECT_DOUBLE_TOLL_DEBUFF, HOST_FESTIVAL, DONATE_CITY, CHARITY

### Task 3: Board enhancements (ctp/core/board.py)

- Constructor signature: thêm `travel_config: Optional[dict] = None` và `map_id: int = 1`
- Attribute: `self.map_id: int = map_id` và `self.travel_config = travel_config`
- Method: `find_nearest_tile_by_space_id(from_pos, space_id) -> int | None` — forward wrap-around search

## Test Results

```
tests/test_models.py: 4 passed
tests/test_events.py: 4 passed
tests/test_board.py: 5 passed
tests/test_fsm.py: passed (pre-existing failures unchanged)
tests/test_tiles.py: passed (pre-existing failures unchanged)
tests/test_config.py: passed (no regressions)
```

Total: 13 new tests added and passing. No regressions introduced.

## Deviations from Plan

### Auto-added Features

**1. [Rule 2 - Missing] Thêm travel_config parameter vào Board constructor**
- **Found during:** Task 3
- **Issue:** Plan chỉ đề cập `map_id` nhưng plan 02/03 sẽ cần `travel_config` để xử lý EF_12 (go-to-travel). Constructor signature trong plan cũng bao gồm `travel_config` — thêm để hoàn chỉnh.
- **Fix:** Thêm `travel_config: Optional[dict] = None` cùng `map_id`
- **Files modified:** ctp/core/board.py
- **Commit:** 766d32e

## Known Stubs

None. Tất cả fields và methods đều functional — không có hardcoded empty values hay placeholders trong code được implement.

## Pre-existing Issues (Out of Scope)

- `tests/test_foundation_fix.py::TestResortPriceScaling::test_resort_toll_uses_base_unit` — `Tile` object có no attribute `visit_count` (pre-existing, không liên quan đến plan này)
- `tests/test_fsm.py` — 4 test failures (pre-existing)
- `tests/test_headless.py` — 4 test failures (pre-existing)

Logged to deferred-items.md scope.

## Self-Check: PASSED

- [x] ctp/core/models.py — tồn tại, có 4 new fields
- [x] ctp/core/events.py — tồn tại, có CARD_EFFECT_ANGEL và 19 constants khác
- [x] ctp/core/board.py — tồn tại, có map_id và find_nearest_tile_by_space_id()
- [x] tests/test_models.py — tồn tại, 4 tests pass
- [x] tests/test_events.py — tồn tại, 4 tests pass
- [x] tests/test_board.py — tồn tại, 5 tests pass
- [x] Commit 78168b6 — verified
- [x] Commit b27797e — verified
- [x] Commit 766d32e — verified

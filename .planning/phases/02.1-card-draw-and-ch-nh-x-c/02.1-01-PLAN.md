---
phase: 02.1-card-draw-and-ch-nh-x-c
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ctp/core/models.py
  - ctp/core/events.py
  - ctp/core/board.py
autonomous: true
requirements: [CARD-01, CARD-02]

must_haves:
  truths:
    - "Player dataclass có 4 fields mới: held_card, accuracy_rate, virus_turns, double_toll_turns"
    - "Board có map_id field và find_nearest_tile_by_space_id() helper"
    - "EventType enum có đủ 20 event types mới cho card effects"
    - "Tất cả test hiện tại vẫn pass sau khi thêm fields (backward compatible)"
  artifacts:
    - path: "ctp/core/models.py"
      provides: "Player with 4 new fields"
      contains: "held_card"
    - path: "ctp/core/events.py"
      provides: "Card effect EventTypes"
      contains: "CARD_EFFECT_ANGEL"
    - path: "ctp/core/board.py"
      provides: "map_id field + find_nearest_tile_by_space_id()"
      contains: "map_id"
  key_links:
    - from: "ctp/tiles/fortune.py"
      to: "ctp/core/models.py"
      via: "player.held_card assignment"
      pattern: "player\\.held_card"
    - from: "ctp/tiles/land.py"
      to: "ctp/core/models.py"
      via: "player.virus_turns + player.double_toll_turns checks"
      pattern: "virus_turns|double_toll_turns"
---

<objective>
Thêm các building blocks cần thiết cho card draw system: 4 Player fields mới, Board.map_id, Board helper method để tìm tile theo loại, và toàn bộ EventType constants cho 23 card effects.

Purpose: Tất cả code ở Plan 02 và 03 phụ thuộc vào các fields/enums này — cần implement trước để không bị import error.
Output: models.py, events.py, board.py đã cập nhật; tất cả test hiện tại vẫn pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-CONTEXT.md
@.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thêm 4 fields mới vào Player dataclass</name>
  <files>ctp/core/models.py, tests/test_models.py</files>
  <behavior>
    - Test 1: Player() khởi tạo không arg vẫn OK, held_card=None, accuracy_rate=15, virus_turns=0, double_toll_turns=0
    - Test 2: Player(player_id="p1", cash=1_000_000) vẫn OK — backward compatible
    - Test 3: Có thể set player.held_card = "IT_CA_3" và đọc lại đúng
    - Test 4: accuracy_rate có thể override: Player(player_id="p1", cash=0, accuracy_rate=25).accuracy_rate == 25
  </behavior>
  <action>
Mở `ctp/core/models.py`. Sau line 30 (`turns_taken: int = 0`), thêm 4 fields sau (per D-09, D-10, D-11, D-12):

```python
    # Phase 02.1: Card draw + Căn lực fields
    held_card: str | None = None          # card_id đang giữ (ví dụ "IT_CA_3"), per D-09
    accuracy_rate: int = 15               # căn lực base rate (%), per D-10
    virus_turns: int = 0                  # EF_7/8 debuff rounds remaining, per D-11
    double_toll_turns: int = 0            # EF_16 self-debuff rounds remaining, per D-12
```

Không thêm method nào. Không thay đổi fields hiện tại.

Sau đó viết tests trong `tests/test_models.py` (nếu file chưa có, tạo mới). Thêm test class `TestPlayerPhase021Fields` với 4 tests theo behavior block trên.
  </action>
  <verify>
    <automated>cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_models.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>4 fields xuất hiện trong Player.__dataclass_fields__, tất cả tests pass, không có existing test bị break</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Thêm 20 EventType constants cho card effects</name>
  <files>ctp/core/events.py, tests/test_events.py</files>
  <behavior>
    - Test 1: EventType.CARD_EFFECT_ANGEL tồn tại và là member của EventType
    - Test 2: Tất cả 20 values mới đều có value khác nhau (không collision với existing)
    - Test 3: EventType.CARD_DRAWN vẫn tồn tại (không bị xóa)
    - Test 4: GameEvent(event_type=EventType.CARD_EFFECT_VIRUS, player_id="p1", data={"target_player": "p2", "duration": 3}) khởi tạo OK
  </behavior>
  <action>
Mở `ctp/core/events.py`. Sau line 45 (`WATER_SLIDE_PUSHED = auto()`), thêm 20 EventType values mới:

```python
    # Phase 02.1: Card effect events
    CARD_EFFECT_ANGEL = auto()               # EF_20: toll waived 100%
    CARD_EFFECT_DISCOUNT_TOLL = auto()       # EF_2: toll 50%
    CARD_EFFECT_SHIELD_BLOCKED = auto()      # EF_3: attack blocked by shield
    CARD_EFFECT_ESCAPE_USED = auto()         # EF_19: prison escape card used
    CARD_EFFECT_PINWHEEL_BYPASS = auto()     # EF_22: bypass elevated tile
    CARD_EFFECT_FORCE_SELL = auto()          # EF_4: force sell opponent tile
    CARD_EFFECT_SWAP_CITY = auto()           # EF_5: swap city ownership
    CARD_EFFECT_DOWNGRADE = auto()           # EF_6/7: downgrade tile level
    CARD_EFFECT_VIRUS = auto()               # EF_7/8/9: virus debuff applied
    CARD_EFFECT_GO_TO_START = auto()         # EF_14: teleport to START
    CARD_EFFECT_GO_TO_PRISON = auto()        # EF_13: teleport to PRISON
    CARD_EFFECT_DOUBLE_TOLL_DEBUFF = auto()  # EF_16: self double toll debuff
    CARD_EFFECT_GO_TO_FESTIVAL = auto()      # EF_10: teleport to FESTIVAL tile
    CARD_EFFECT_GO_TO_FESTIVAL_TILE = auto() # EF_11: teleport to tile with festival marker
    CARD_EFFECT_GO_TO_TRAVEL = auto()        # EF_12: teleport to TRAVEL tile
    CARD_EFFECT_GO_TO_TAX = auto()           # EF_26: teleport to TAX tile
    CARD_EFFECT_GO_TO_GOD = auto()           # EF_21: teleport to nearest GOD tile
    CARD_EFFECT_HOST_FESTIVAL = auto()       # EF_15: set festival marker free
    CARD_EFFECT_DONATE_CITY = auto()         # EF_17: donate tile to another player
    CARD_EFFECT_CHARITY = auto()             # EF_18: charity — all pay poorest
```

Thêm tests vào `tests/test_events.py` (nếu chưa có, tạo mới với class `TestEventTypePhase021`).
  </action>
  <verify>
    <automated>cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_events.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>Tất cả 20 EventType mới tồn tại, không có value collision, test_events pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Thêm map_id field và find_nearest_tile_by_space_id() vào Board</name>
  <files>ctp/core/board.py, tests/test_board.py</files>
  <behavior>
    - Test 1: Board khởi tạo không có map_id arg → board.map_id == 1 (default, backward compatible)
    - Test 2: Board khởi tạo với map_id=2 → board.map_id == 2
    - Test 3: find_nearest_tile_by_space_id(from_pos=1, space_id=SpaceId.PRISON) trên Map 1 trả về position 9 (PRISON ở pos 9)
    - Test 4: find_nearest_tile_by_space_id(from_pos=15, space_id=SpaceId.TAX) tìm TAX tile theo hướng tiến từ pos 15
    - Test 5: find_nearest_tile_by_space_id với space_id không tồn tại trên board trả về None
  </behavior>
  <action>
Mở `ctp/core/board.py`.

**Bước 1: Thêm `map_id` parameter vào constructor.**

Tại line 61, sửa signature `def __init__(self, space_positions, land_config, ...)`:
```python
    def __init__(
        self,
        space_positions: dict[str, dict],
        land_config: dict,
        resort_config: Optional[dict] = None,
        festival_config: Optional[dict] = None,
        prison_config: Optional[dict] = None,
        travel_config: Optional[dict] = None,
        map_id: int = 1,          # [NEW per D-02] 1=Map1, 2=Map2, 3=Map3
    ):
```

Trong body constructor (sau line `self.water_wave = ...`), thêm:
```python
        self.map_id: int = map_id  # map_id để filter card mapNotAvail
```

**Bước 2: Thêm method `find_nearest_tile_by_space_id()`.**

Sau method `get_festival_config()` (khoảng line 153), thêm method mới:

```python
    def find_nearest_tile_by_space_id(
        self, from_pos: int, space_id: SpaceId
    ) -> int | None:
        """Tìm tile gần nhất theo chiều tiến từ from_pos có space_id khớp.

        Tìm kiếm theo hướng tiến (forward), wrap-around, tìm trong tối đa 32 bước.
        Không tính ô from_pos chính nó.

        Args:
            from_pos: Vị trí xuất phát (1-32).
            space_id: Loại tile cần tìm (SpaceId enum).

        Returns:
            Position (1-32) của tile gần nhất, hoặc None nếu không có.
        """
        for steps in range(1, 33):
            candidate = ((from_pos - 1 + steps) % 32) + 1
            tile = self.get_tile(candidate)
            if tile.space_id == space_id:
                return candidate
        return None
```

Thêm tests vào `tests/test_board.py` — class `TestBoardPhase021`.
  </action>
  <verify>
    <automated>cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_board.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <done>board.map_id accessible, find_nearest_tile_by_space_id() trả về đúng position, tất cả test pass</done>
</task>

</tasks>

<verification>
Sau khi hoàn thành tất cả tasks:

```bash
cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/ -x -q 2>&1 | tail -10
```

Không có test nào fail. Không có import error. 4 Player fields tồn tại, 20 EventType tồn tại, Board.map_id tồn tại, find_nearest_tile_by_space_id() hoạt động.
</verification>

<success_criteria>
- `Player().held_card is None` và `Player().accuracy_rate == 15`
- `EventType.CARD_EFFECT_ANGEL` và 19 constants khác tồn tại trong EventType enum
- `Board(space_positions, land_config).map_id == 1` (default)
- `Board.find_nearest_tile_by_space_id(1, SpaceId.PRISON)` trả về 9 trên Map 1 config
- Tất cả pre-existing tests (`tests/test_fsm.py`, `tests/test_tiles.py`, `tests/test_config.py`) vẫn pass
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-01-SUMMARY.md` với:
- Files đã sửa và thay đổi cụ thể
- Line numbers của các additions
- Test results
</output>

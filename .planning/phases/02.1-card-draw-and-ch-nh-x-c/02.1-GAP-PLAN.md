---
phase: 02.1-card-draw-and-ch-nh-x-c
plan: GAP
type: execute
wave: 1
depends_on: ["02.1-01", "02.1-02", "02.1-03"]
files_modified:
  - ctp/core/models.py
  - ctp/tiles/_toll_modifiers.py
  - ctp/tiles/fortune.py
  - ctp/controller/fsm.py
  - tests/test_cards.py
  - tests/test_fsm.py
  - tests/test_models.py
autonomous: true
requirements: [CARD-03]
gap_closure: true

must_haves:
  truths:
    - "Player dataclass has virus_turns: int = 0 field accessible after construction"
    - "When owner has virus_turns > 0, visitor pays 0 toll and owner.virus_turns resets to 0"
    - "EF_7/EF_8 cards set opponent.virus_turns = 3 (player-level, not tile-level)"
    - "virus_turns decrements by 1 for all debuffed players at END_TURN"
  artifacts:
    - path: "ctp/core/models.py"
      provides: "virus_turns field on Player"
      contains: "virus_turns: int = 0"
    - path: "ctp/tiles/_toll_modifiers.py"
      provides: "Toll modifier checks owner.virus_turns"
      contains: "virus_turns"
    - path: "ctp/controller/fsm.py"
      provides: "virus_turns decrement in _do_end_turn"
      contains: "virus_turns"
  key_links:
    - from: "ctp/tiles/fortune.py"
      to: "Player.virus_turns"
      via: "EF_7/EF_8 sets opponent.virus_turns = 3"
      pattern: "virus_turns.*=.*3"
    - from: "ctp/tiles/_toll_modifiers.py"
      to: "Player.virus_turns"
      via: "Step 1 checks owner.virus_turns > 0"
      pattern: "owner.*virus_turns"
    - from: "ctp/controller/fsm.py"
      to: "Player.virus_turns"
      via: "END_TURN decrements virus_turns"
      pattern: "virus_turns.*-=.*1"
---

<objective>
Fix virus debuff to use player-level `virus_turns` field per D-11 spec instead of tile-level `toll_debuff_turns`.

Purpose: UAT test 3 failed — `Player.virus_turns` field is missing entirely. The virus mechanic was implemented as tile-level debuff (`tile.toll_debuff_turns`) but D-11/D-22/D-44 spec requires player-level semantics: owner has virus for 3 turns affecting ALL their tiles, cleared early when any visitor lands.
Output: Player.virus_turns field exists, toll modifiers check owner.virus_turns, EF_7/EF_8 set opponent.virus_turns, END_TURN decrements, all tests pass.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-CONTEXT.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From ctp/core/models.py (Player dataclass, line 34 — add virus_turns after double_toll_turns):
```python
held_card: str | None = None          # card_id dang giu, per D-09
accuracy_rate: int = 15               # can luc base rate (%), per D-10
double_toll_turns: int = 0            # EF_16 self-debuff rounds remaining, per D-12
# MISSING: virus_turns: int = 0      # EF_7/8 debuff rounds remaining, per D-11
```

From ctp/tiles/_toll_modifiers.py (apply_toll_modifiers signature):
```python
def apply_toll_modifiers(
    player: Player, owner: Optional[Player], tile, rent: float, event_bus
) -> tuple[float, bool]:
```
Step 1 currently checks `tile.toll_debuff_turns` — must change to `owner.virus_turns`.

From ctp/tiles/fortune.py (_apply_color_pair_debuff, line 304-336):
- Currently sets `tile.toll_debuff_turns = 5` on each opponent tile
- Must change to `opponent.virus_turns = 3` (player-level)

From ctp/controller/fsm.py (_do_end_turn, line 835-840):
- Currently decrements `tile.toll_debuff_turns` for all tiles
- Must ADD decrement of `player.virus_turns` for all players
- Keep tile-level decrement as-is (tile fields still exist on Tile dataclass)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add virus_turns field and fix toll modifier + fortune card logic</name>
  <files>ctp/core/models.py, ctp/tiles/_toll_modifiers.py, ctp/tiles/fortune.py, ctp/controller/fsm.py, tests/test_models.py, tests/test_cards.py, tests/test_fsm.py</files>
  <behavior>
    - Test: Player() default has virus_turns == 0
    - Test: Player(virus_turns=3) stores value correctly
    - Test: apply_toll_modifiers with owner.virus_turns > 0 returns (0.0, True) and sets owner.virus_turns = 0
    - Test: apply_toll_modifiers with owner.virus_turns == 0 does NOT skip toll
    - Test: EF_7 card sets opponent.virus_turns = 3 (not tile.toll_debuff_turns)
    - Test: EF_8 card sets opponent.virus_turns = 3 (not tile.toll_debuff_turns)
    - Test: _do_end_turn decrements player.virus_turns for all debuffed players
    - Test: priority order — owner.virus_turns > 0 beats double_toll (toll = 0)
  </behavior>
  <action>
    **1. models.py** — Add field after `double_toll_turns` (line 34):
    ```python
    virus_turns: int = 0                  # EF_7/8 owner debuff rounds remaining, per D-11
    ```

    **2. _toll_modifiers.py** — Rewrite Step 1 per D-44:
    - Change Step 1 from checking `tile.toll_debuff_turns` to checking `owner.virus_turns > 0`
    - When owner has virus: return `(0.0, True)` AND set `owner.virus_turns = 0` (clear early per D-11/D-22)
    - Emit `CARD_EFFECT_VIRUS` event with data `{"owner": owner.player_id, "cleared_early": True}`
    - Keep the `owner: Optional[Player]` param — guard with `if owner is not None and owner.virus_turns > 0`
    - Remove the `tile.toll_debuff_turns` check from Step 1 entirely (tile-level debuff fields stay on Tile dataclass but are no longer checked in toll modifiers)

    **3. fortune.py** — Change `_apply_color_pair_debuff()`:
    - Replace the loop `for tile in affected_tiles: tile.toll_debuff_turns = 5; tile.toll_debuff_rate = debuff_rate` with `opponent.virus_turns = 3`
    - Remove `affected_tiles` logic — virus is player-level, not tile-level
    - Simplify: after shield check, just set `opponent.virus_turns = 3`
    - Update event data: `{"target_player": opponent.player_id, "duration": 3}` (remove "tiles" and "debuff_rate")
    - Note: EF_7 and EF_8 now have SAME effect (virus_turns=3) since D-11 says virus = no toll on ALL tiles. The distinction (0% vs 50%) was tile-level — at player-level it is always "no toll". If this seems wrong, still implement per D-11/D-44 spec which says toll = 0 when owner has virus.

    **4. fsm.py** — In `_do_end_turn()`, ADD virus_turns decrement for all players BEFORE the existing tile-level debuff decrement block (line 835):
    ```python
    # [D-46] Decrement virus_turns for all debuffed players
    for p in self.players:
        if p.virus_turns > 0:
            p.virus_turns -= 1
    ```
    Keep the existing tile-level `toll_debuff_turns` decrement block as-is (it does no harm, tiles still have those fields).

    **5. Update tests:**
    - `tests/test_models.py`: Add test that Player default has `virus_turns == 0`
    - `tests/test_cards.py` TestTollModifiers: Update `test_virus_skip_toll` to set `owner.virus_turns = 3` instead of `tile.toll_debuff_turns = 3`. Assert owner.virus_turns == 0 after (cleared early). Same for `test_priority_order_virus_beats_double_toll`. Update `test_resort_virus_skip_toll` similarly.
    - `tests/test_cards.py` TestFortuneCards: Update `test_ef7_virus_on_land` to assert `opponent.virus_turns == 3` instead of `city_tile.toll_debuff_turns == 5`. Update `test_ef8_yellow_sand_on_land` similarly.
    - `tests/test_cards.py`: Update `test_yellow_sand_half_toll` — with player-level virus, EF_8 also means toll=0 (not 50%), so this test should assert full toll skip (or remove if redundant with virus_skip_toll test).
    - `tests/test_fsm.py`: Update `test_toll_debuff_turns_decrements_in_end_turn` to test player.virus_turns decrement instead. Update `test_toll_debuff_turns_clears_rate_when_expired` similarly.
  </action>
  <verify>
    <automated>python -m pytest tests/test_models.py tests/test_cards.py tests/test_fsm.py -v --tb=short -x</automated>
  </verify>
  <done>
    - Player.virus_turns field exists with default 0
    - Toll modifier Step 1 checks owner.virus_turns (not tile.toll_debuff_turns)
    - EF_7/EF_8 set opponent.virus_turns = 3
    - _do_end_turn decrements virus_turns for all players
    - All existing tests updated and passing
    - No regressions in full test suite
  </done>
</task>

<task type="auto">
  <name>Task 2: Full regression and smoke test</name>
  <files></files>
  <action>
    Run the complete test suite to verify no regressions from the virus mechanic change:
    ```bash
    python -m pytest tests/ -v --tb=short
    ```

    Then run headless smoke test:
    ```bash
    python main.py --headless
    ```

    If any failures, diagnose and fix. The most likely regressions:
    - Any test that directly sets `tile.toll_debuff_turns` expecting toll skip behavior (these should have been caught in Task 1 test updates)
    - Fortune card tests that assert tile-level debuff state after EF_7/EF_8
  </action>
  <verify>
    <automated>python -m pytest tests/ -v --tb=short && python main.py --headless</automated>
  </verify>
  <done>
    - All tests pass (0 failures)
    - Headless game runs to completion without exceptions
    - virus_turns field confirmed accessible on Player objects during game
  </done>
</task>

</tasks>

<verification>
1. `python -m pytest tests/test_models.py -k virus -v` — virus_turns field exists on Player
2. `python -m pytest tests/test_cards.py -k virus -v` — toll modifier and fortune card virus tests pass
3. `python -m pytest tests/test_fsm.py -k debuff -v` — END_TURN virus decrement works
4. `python -m pytest tests/ -v --tb=short` — full suite, 0 failures
5. `python main.py --headless` — game completes without crash
</verification>

<success_criteria>
- Player.virus_turns: int = 0 exists in models.py
- apply_toll_modifiers checks owner.virus_turns per D-44 Step 1
- EF_7/EF_8 in fortune.py set opponent.virus_turns = 3 per D-22
- _do_end_turn decrements virus_turns per D-46
- All tests pass including updated virus-related tests
- Headless game runs without errors
</success_criteria>

<output>
After completion, create `.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-GAP-SUMMARY.md`
</output>

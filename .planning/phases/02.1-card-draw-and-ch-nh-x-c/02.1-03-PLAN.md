---
phase: 02.1-card-draw-and-ch-nh-x-c
plan: 03
type: execute
wave: 3
depends_on: [02.1-01, 02.1-02]
files_modified:
  - ctp/controller/fsm.py
  - tests/test_fsm.py
autonomous: true
requirements: [CARD-02, CARD-05]

must_haves:
  truths:
    - "Đổ chính xác (căn lực) hoạt động: 15% cơ hội AI hit khoảng đã chọn"
    - "AI chọn khoảng lực target-aware: ưu tiên unowned CITY/RESORT gần nhất"
    - "FSM ROLL phase thực hiện đúng thứ tự D-42: EF_19 escape → decrement double_toll → căn lực → roll"
    - "FSM MOVE phase check EF_22 Pinwheel trước elevated tile block (D-43)"
    - "FSM END_TURN decrement virus_turns cho tất cả players (D-46)"
    - "Bug dict keys trong _do_move() đã được fix"
    - "DICE_ROLL event có thêm chosen_range và precision_hit fields"
  artifacts:
    - path: "ctp/controller/fsm.py"
      provides: "_resolve_can_luc(), _choose_range_ai(), modified _do_roll(), _do_move(), _do_end_turn()"
      contains: "_resolve_can_luc"
  key_links:
    - from: "ctp/controller/fsm.py"
      to: "ctp/core/models.py"
      via: "player.accuracy_rate trong precision check"
      pattern: "accuracy_rate"
    - from: "ctp/controller/fsm.py"
      to: "ctp/core/events.py"
      via: "DICE_ROLL event với precision metadata"
      pattern: "chosen_range.*precision_hit"
---

<objective>
Implement Đổ Chính Xác (Căn Lực) mechanic và tích hợp tất cả card effects vào FSM phases: EF_19 escape check trong ROLL, EF_22 Pinwheel bypass trong MOVE, virus_turns decrement trong END_TURN. Fix bug dict keys trong _do_move().

Purpose: Phase này kết nối tất cả pieces từ Plan 01/02 vào game loop thực tế — sau plan này ván đấu chạy hoàn chỉnh với card effects và căn lực.
Output: fsm.py với 3 new methods + modifications to _do_roll, _do_move, _do_end_turn.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-CONTEXT.md
@.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-RESEARCH.md
@.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-01-SUMMARY.md
@.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-02-SUMMARY.md

<interfaces>
<!-- Từ ctp/core/models.py (sau Plan 01) -->
```python
@dataclass
class Player:
    held_card: str | None = None
    accuracy_rate: int = 15
    virus_turns: int = 0
    double_toll_turns: int = 0
    prison_turns_remaining: int = 0
    def enter_prison() -> None
    def exit_prison() -> None
```

<!-- Từ ctp/controller/fsm.py (hiện tại) -->
Relevant existing methods:
  _do_roll() — lines 126-198: pending_travel → prison_turns → roll_dice() → doubles → MOVE
  _do_move() — lines 380-504: elevated check (BUG dict keys 419-432) → water wave → move
  _do_end_turn() — lines 652-719: turns_taken += 1 → win check → advance player
  roll_dice() — called at line 162, returns tuple[int,int], sets self._current_dice
  self._rolled_doubles: bool
  self._doubles_streak: int
  self.current_player: Player
  self.board: Board
  self.players: list[Player]

<!-- Bug location (RISK-06) -->
Lines 422-432 trong _do_move() — dict keys bare names, phải là strings:
  data={position: elevated_pos}              → data={"position": elevated_pos}
  data={old_pos: old_pos, new_pos: elevated_pos, ...} → data={"old_pos": ..., "new_pos": ..., ...}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _resolve_can_luc() và _choose_range_ai() trong FSM</name>
  <files>ctp/controller/fsm.py, tests/test_fsm.py</files>
  <behavior>
    - Test 1: _resolve_can_luc(chosen_range=0) với mock random trả về 10 (> 15) → precision_hit=False, dice là 2d6 bình thường
    - Test 2: _resolve_can_luc(chosen_range=0) với mock random trả về 5 (≤ 15) → precision_hit=True, dice sum trong [2,3,4]
    - Test 3: _resolve_can_luc(chosen_range=3) với precision hit → precision_hit=True, dice sum trong [10,11,12]
    - Test 4: dice split (D-40): T=11 → d1 ∈ [5,6], d2=11-d1, cả hai trong [1,6]
    - Test 5: doubles đặc biệt: T=6 → có thể trả về (3,3,True) — doubles logic hoạt động
    - Test 6: _choose_range_ai() với board không có unowned tile → trả về 1 (fallback)
    - Test 7: _choose_range_ai() với unowned CITY ở 6 steps → trả về range index chứa 6 (range 1: [5-7])
  </behavior>
  <action>
Mở `ctp/controller/fsm.py`. Thêm import `import random` nếu chưa có ở đầu file.

Thêm constant sau imports (trước class GameController):
```python
# Căn lực: 4 khoảng xúc xắc (D-37)
_CAN_LUC_RANGES = [
    (2, 4),    # Khoảng 0: low
    (5, 7),    # Khoảng 1: mid-low (default fallback)
    (7, 9),    # Khoảng 2: mid-high (7 overlap với khoảng 1 — by design)
    (10, 12),  # Khoảng 3: high
]
```

Thêm 2 methods vào class GameController (trước `_do_roll()`):

```python
    def _choose_range_ai(self) -> int:
        """Chọn khoảng lực tối ưu dựa trên vị trí đích (stub AI per D-39).

        Strategy:
        1. Tìm unowned CITY/RESORT gần nhất (ưu tiên opt cao hơn).
        2. Nếu steps nằm trong khoảng nào đó → ưu tiên khoảng đó.
        3. Nếu khoảng ưu tiên trùng với đất đối thủ → giảm ưu tiên.
        4. Trong các khoảng còn lại → ưu tiên khoảng chứa doubles values.
        5. Fallback: khoảng 1 [5-7].

        Returns:
            Index của khoảng (0-3).
        """
        player = self.current_player
        board = self.board
        # Tìm unowned CITY/RESORT gần nhất (trong 12 bước)
        best_step = None
        best_opt = -1
        for steps in range(1, 13):
            target_pos = ((player.position - 1 + steps) % 32) + 1
            tile = board.get_tile(target_pos)
            if tile.space_id in (SpaceId.CITY, SpaceId.RESORT) and tile.owner_id is None:
                if best_step is None or tile.opt > best_opt:
                    best_step = steps
                    best_opt = tile.opt
                    break  # first unowned, stop

        if best_step is None:
            return 1  # Fallback

        # Tìm khoảng khớp
        for i, (lo, hi) in enumerate(_CAN_LUC_RANGES):
            if lo <= best_step <= hi:
                # Kiểm tra target có phải đất đối thủ không
                target_pos = ((player.position - 1 + best_step) % 32) + 1
                tile = board.get_tile(target_pos)
                if tile.owner_id and tile.owner_id != player.player_id:
                    continue  # bỏ qua khoảng này
                return i

        # Fallback: khoảng có doubles (6 và 8 là doubles values trong các khoảng)
        for i, (lo, hi) in enumerate(_CAN_LUC_RANGES):
            doubles_in_range = [v for v in range(lo, hi + 1) if v % 2 == 0 and 2 <= v <= 12]
            if doubles_in_range:
                return i
        return 1

    def _resolve_can_luc(self, chosen_range: int) -> tuple[int, int, bool]:
        """Precision dice roll cho căn lực (D-38).

        15% cơ hội hit khoảng đã chọn (player.accuracy_rate).
        Nếu hit: random T trong [lo, hi], split theo D-40.
        Nếu miss: normal 2d6.

        Args:
            chosen_range: Index khoảng lực (0-3).

        Returns:
            (d1, d2, precision_hit) — hit=True nếu precision roll thành công.
        """
        lo, hi = _CAN_LUC_RANGES[chosen_range]
        precision_check = random.randint(1, 100)
        if precision_check <= self.current_player.accuracy_rate:
            # Hit: random T trong khoảng, split thành (d1, d2) per D-40
            T = random.randint(lo, hi)
            d1_lo = max(1, T - 6)
            d1_hi = min(6, T - 1)
            d1 = random.randint(d1_lo, d1_hi)
            d2 = T - d1
            return (d1, d2, True)
        else:
            # Miss: normal 2d6
            return (random.randint(1, 6), random.randint(1, 6), False)
```
  </action>
  <verify>
    <automated>cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_fsm.py -x -q -k "can_luc or range_ai" 2>&1 | tail -10</automated>
  </verify>
  <done>_resolve_can_luc() trả về dice sum trong khoảng khi precision hit, normal 2d6 khi miss, _choose_range_ai() trả về valid index 0-3</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: FSM integration — sửa _do_roll(), _do_move(), _do_end_turn() + fix dict key bug</name>
  <files>ctp/controller/fsm.py, tests/test_fsm.py</files>
  <behavior>
    - Test 1: player có prison_turns_remaining=2 và held_card="IT_CA_21" (EF_19) → khi _do_roll(), card được consume, player thoát tù, double_toll_turns KHÔNG bị decrement trước prison check
    - Test 2: player.double_toll_turns=1 → sau _do_roll() kết thúc (không trong tù), double_toll_turns == 0
    - Test 3: DICE_ROLL event data có "chosen_range" và "precision_hit" fields
    - Test 4: player có held_card="IT_CA_23" (EF_22) và có elevated tile trong đường đi → held_card = None sau _do_move(), board.elevated_tile = None, player di chuyển bình thường
    - Test 5: Bug fix — TILE_LOWERED event data["position"] có giá trị đúng (không phải variable reference)
    - Test 6: _do_end_turn() với player có virus_turns=2 → sau end_turn, player.virus_turns == 1 (giảm 1)
    - Test 7: PLAYER_MOVE event data có "old_pos" và "new_pos" là integers (not variable names)
  </behavior>
  <action>
**Bước 1: Fix dict key bug trong _do_move() (RISK-06, HIGH PRIORITY)**

Mở `ctp/controller/fsm.py`. Tại khoảng line 419-432, sửa 2 GameEvent publishes có bare variable names làm dict keys:

```python
# TRƯỚC (line ~422, BUG):
            self.event_bus.publish(GameEvent(
                event_type=EventType.TILE_LOWERED,
                player_id=self.current_player.player_id,
                data={position: elevated_pos}   # BUG: bare variable
            ))
            self.event_bus.publish(GameEvent(
                event_type=EventType.PLAYER_MOVE,
                player_id=self.current_player.player_id,
                data={
                    old_pos: old_pos,           # BUG: bare variable
                    new_pos: elevated_pos,       # BUG: bare variable
                    passed_start: self._passed_start,  # BUG
                    blocked_by_elevated: True,   # BUG
                }
            ))

# DEPOIS (FIX):
            self.event_bus.publish(GameEvent(
                event_type=EventType.TILE_LOWERED,
                player_id=self.current_player.player_id,
                data={"position": elevated_pos}
            ))
            self.event_bus.publish(GameEvent(
                event_type=EventType.PLAYER_MOVE,
                player_id=self.current_player.player_id,
                data={
                    "old_pos": old_pos,
                    "new_pos": elevated_pos,
                    "passed_start": self._passed_start,
                    "blocked_by_elevated": True,
                }
            ))
```

**Bước 2: Sửa _do_roll() — thêm EF_19 escape check, double_toll decrement, căn lực**

Sửa method `_do_roll()` (line 126). Thêm import SpaceId nếu chưa có. Thêm code ngay sau block `if self.current_player.pending_travel:` (line 132-135) và trước block `if self.current_player.prison_turns_remaining > 0:` (line 137):

```python
        # [NEW D-42 Step A] EF_19 Escape card — auto-use khi ở tù (D-16)
        if (self.current_player.prison_turns_remaining > 0
                and self.current_player.held_card is not None):
            from ctp.tiles.fortune import _load_raw_card_data  # lazy import tránh circular
            raw = _load_raw_card_data()
            held_effect = raw.get(self.current_player.held_card, {}).get("effect", "")
            if held_effect == "EF_19":
                self.current_player.held_card = None  # consume (D-08)
                self.current_player.exit_prison()     # prison_turns = 0
                self.event_bus.publish(GameEvent(
                    event_type=EventType.CARD_EFFECT_ESCAPE_USED,
                    player_id=self.current_player.player_id,
                    data={}
                ))
                # Tiếp tục roll bình thường (không return, fall through)

        # [NEW D-42 Step B] Decrement double_toll_turns ở đầu ROLL phase
        if self.current_player.double_toll_turns > 0:
            self.current_player.double_toll_turns -= 1
```

Sau đó sửa phần `dice = self.roll_dice()` (line 162) để thêm căn lực. **Thay thế** đoạn từ line 162 đến `self.event_bus.publish(GameEvent(event_type=EventType.DICE_ROLL, ...))`:

```python
        # [NEW D-42 Step C] AI chọn khoảng lực
        chosen_range = self._choose_range_ai()
        # [NEW D-42 Step D] Precision roll — returns (d1, d2, hit)
        d1, d2, precision_hit = self._resolve_can_luc(chosen_range)
        dice = (d1, d2)

        # Set dice result
        self._current_dice = dice
        self._rolled_doubles = (dice[0] == dice[1])

        if self._rolled_doubles:
            self._doubles_streak += 1
        else:
            self._doubles_streak = 0

        self.event_bus.publish(GameEvent(
            event_type=EventType.DICE_ROLL,
            player_id=self.current_player.player_id,
            data={
                "dice": dice,
                "total": sum(dice),
                "doubles": self._rolled_doubles,
                "doubles_streak": self._doubles_streak,
                "chosen_range": chosen_range,     # [NEW]
                "precision_hit": precision_hit,   # [NEW]
            }
        ))
```

**Quan trọng:** Xóa dòng `dice = self.roll_dice()` cũ (line 162) và phần publish DICE_ROLL cũ (lines 170-175) vì đã được thay thế ở trên.

**Bước 3: Sửa _do_move() — thêm EF_22 Pinwheel bypass trước elevated tile block**

Tìm dòng `elevated_pos = self.board.find_elevated_in_path(old_pos, dice_total)` (line 394). Ngay SAU dòng đó, thêm:

```python
        # [NEW D-43] EF_22 Pinwheel bypass — bỏ qua elevated tile nếu player giữ Pinwheel card
        if elevated_pos is not None and self.current_player.held_card is not None:
            from ctp.tiles.fortune import _load_raw_card_data
            raw = _load_raw_card_data()
            held_effect = raw.get(self.current_player.held_card, {}).get("effect", "")
            if held_effect == "EF_22":
                self.current_player.held_card = None   # consume (D-08)
                self.board.elevated_tile = None         # clear elevated state
                elevated_pos = None                     # skip elevated block
                self.event_bus.publish(GameEvent(
                    event_type=EventType.CARD_EFFECT_PINWHEEL_BYPASS,
                    player_id=self.current_player.player_id,
                    data={}
                ))
```

**Bước 4: Sửa _do_end_turn() — thêm virus_turns decrement**

Tìm `self._advance_to_next_player()` trong `_do_end_turn()` (khoảng line 702). Ngay TRƯỚC dòng này, thêm:

```python
        # [NEW D-46] Decrement virus_turns cho tất cả players sau mỗi END_TURN
        for p in self.players:
            if p.virus_turns > 0:
                p.virus_turns -= 1
```
  </action>
  <verify>
    <automated>cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_fsm.py -x -q 2>&1 | tail -15</automated>
  </verify>
  <done>
    - DICE_ROLL event có chosen_range và precision_hit
    - EF_19 escape card được trigger tự động khi ở tù
    - double_toll_turns decrements ở đầu ROLL
    - EF_22 Pinwheel bypass hoạt động trong MOVE
    - virus_turns decrements trong END_TURN
    - Dict key bug đã fix, không còn bare variable keys
    - Tất cả existing FSM tests pass
  </done>
</task>

</tasks>

<verification>
```bash
cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/ -x -q 2>&1 | tail -20
```

Kiểm tra thêm:
```bash
cd D:/workspace/CTP/CTPPrototype && python -c "
from ctp.controller.fsm import GameController, _CAN_LUC_RANGES
print('_CAN_LUC_RANGES:', _CAN_LUC_RANGES)
print('OK: FSM imports cleanly')
"
```

Integration smoke test:
```bash
cd D:/workspace/CTP/CTPPrototype && python -m pytest tests/test_fsm.py tests/test_cards.py tests/test_tiles.py -q 2>&1 | tail -10
```
</verification>

<success_criteria>
- `_CAN_LUC_RANGES[0] == (2, 4)` và `_CAN_LUC_RANGES[3] == (10, 12)`
- Với player.accuracy_rate=100: `_resolve_can_luc(chosen_range=2)` luôn trả về sum trong [7,9]
- DICE_ROLL event data có keys "chosen_range" và "precision_hit"
- Sau _do_end_turn(): tất cả players có virus_turns > 0 đã giảm 1
- Player với held_card EF_19 trong tù: sau _do_roll(), prison_turns_remaining == 0
- TILE_LOWERED event data["position"] là integer (không phải variable reference)
- `python -m pytest tests/ -q` — tất cả tests pass, không có failure
</success_criteria>

<output>
Sau khi hoàn thành, tạo `.planning/phases/02.1-card-draw-and-ch-nh-x-c/02.1-03-SUMMARY.md` với:
- Methods đã thêm/sửa trong fsm.py và line numbers
- Bug fixes đã thực hiện
- Test results đầy đủ
</output>
